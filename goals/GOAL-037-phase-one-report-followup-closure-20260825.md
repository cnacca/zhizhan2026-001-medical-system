# 一期测试报告剩余反馈收口

<!-- repo-init:managed -->

## Metadata

- ID: `GOAL-037`
- Status: `completed`
- Type: `milestone`
- Mode: `repo-hydrate`
- Source: `《2026.8.22 软件测试报告》问题线索与 2026-08-25 用户范围确认`
- Created: `2026-08-25`
- Updated: `2026-08-25`

## Goal

在不删除医生下单字段、不放宽提交校验、不改写已发布目录和历史订单快照的前提下，完成两项一期反馈：

1. 将医生下单向导从当前 6 个可见步骤精简为 4 个可见步骤；
2. 补齐产品目录中“螺旋扩弓器（单向）/（双向）”的准确表达；“推簧”按 D-214 暂缓。

其中原清单第 1 项先实施，原清单第 4 项排在其后。完整 CRM／客户关怀提醒和 STL 直接打开 CAD／设计软件由用户明确转入二期，本 Goal 不实施。

## Scope

- 医生多产品下单向导的步骤编排、步骤标题、前进／后退、缺项定位和中英文文案。
- 现有患者、产品、牙位、制作要求、材料工艺、资料上传、试戴／过程确认、价格／周期确认等字段和校验的重新分组。
- 草稿恢复、多产品切换、共享／专属资料、动态表单、正畸处方和最终确认回归。
- 设计服务原动态下单表制作要求的来源复核、医生端字段、目录规则和服务端提交校验。
- 已发布产品目录、源基线、螺旋扩弓方向，以及“推簧／弹簧矫正器／加弹簧”的来源边界核对；“推簧”不进入当前实现。
- 目标测试、前端构建、隔离浏览器回归和项目文档回写。

## Non-goals

- 不因步骤合并删除字段或放宽前后端业务校验；用户另行确认的 D-210 备注选填与 D-212 三项制作要求恢复按后续决策执行。
- 不改变单文件 500MB、单订单最多 50 个、文件类型、MinIO、Multipart、签名 URL、权限或审计边界。
- 不实现完整 CRM、客户关怀提醒或客服工单系统；这些进入二期。
- 不实现 STL 直接打开 CAD／设计软件；外部协议、客户端、授权与审计在二期另行定义。
- 不在缺少来源依据时把“推簧”直接重命名为现有产品，也不创建重复 SKU。
- 不修改正式业务数据，不提交、推送或部署，不把 Task 8 标记为 `READY`。

## Acceptance

- 医生端只显示 4 个步骤：`基础信息与产品`、`制作配置`、`资料上传`、`复核与提交`。
- 原 6 步内容按 `1 → 1`、`2 + 3 → 2`、`4 → 3`、`5 + 6 → 4` 合并，现有字段、动态表单、正畸处方、文件槽位和最终确认信息无丢失。
- “下一步”、返回、草稿恢复、缺项清单和“去填写”均使用新步骤编号并能定位到真实可修改区域。
- 多产品草稿、共享／专属资料覆盖、扫描资料完整性和最终提交服务端门禁保持原语义。
- 中英文步骤标题和缺项提示同步，窄屏与桌面布局无新增阻断。
- 设计服务必须选择数据格式 `STL / OBJ / EXO / 3SHAPE`、设计标准“通用／个性化”和设计时间“12 小时／24 小时／3 天”；设计备注继续选填。
- “推簧”已完成来源与术语核对并按 D-214 记为 `USER_DEFERRED`；用户恢复前不进入目录草稿、别名或发布流程。
- 螺旋扩弓器保留单一产品并以必选规格区分单向／双向；服务端拒绝空规格，订单快照冻结方向。
- Task 8 继续保持 `NOT_READY`。

## Verification

- `npm run check:doctor-order-product-first`
- `npm run check:doctor-order-final-confirmation`
- `npm run check:doctor-portal-i18n`
- `npm run build:frontend`
- 医生下单隔离浏览器回归：四步导航、缺项定位、草稿恢复、多产品、资料上传和最终确认
- `npm run check:phase-one-workflow`
- `npm run check:stage-goal-window`
- `npm run check:repoframe-docs`
- `npm run check:acceptance-baseline`
- `npm run acceptance`
- `git diff --check`

## Planned Task

- `tasks/TASK-039-doctor-order-simplification-and-catalog-mapping-20260825.md`

## Assumption Checks

### Validated

- 当前医生下单向导确实定义并展示 6 个步骤，页面校验和缺项定位仍存在多个硬编码步骤编号。
- 当前已发布目录存在“弹簧矫正器”“螺旋扩弓器”，动态下单来源中存在配件“加弹簧”，但没有可直接证明等价关系的“推簧”显示名。
- 现有业务信息可通过合并页面分组精简导航，无需删除字段或修改后端数据模型。
- 原 DOCX 指纹、XML 与渲染第 4 页均明确支持设计服务三项制作要求；后续界面示例中的 6／48 小时不覆盖主来源。

### Closed Locally

- D-213 已将 D-211 标记为 `REOPENED / NOT_APPROVED_FOR_COMMIT`。
- D-215 已用 V95 产品变体补齐螺旋扩弓方向，并清除 D-211 的错误别名实现。
- “推簧”是真实术语但原表没有定义其目录身份，按 D-214 暂缓，不作为当前阻塞项。
- 提交、发布和正式站只读复验不改变 Task 8 `NOT_READY`。

## Downstream Impact

- 步骤编号变化会影响页面分支、校验摘要、服务端错误映射、草稿恢复和浏览器回归，实施时必须统一迁移，不能只改步骤标题。
- 目录映射必须继续走版本化草稿／发布和历史订单快照边界，不直接修改已发布版本或历史订单。
- 二期新增 CRM／客户关怀和 CAD 直连时，应复用现有客户档案、消息、文件权限和审计能力，但不得反向扩大本 Goal。

## Completion Record

- 2026-08-25：用户确认第 2、3 项转入二期，第 1、4 项保留一期，并要求先整理文档、准备实施第 1 项。
- 2026-08-25：完成 6 → 4 步实施基线、回归矩阵和第 4 项映射门槛；本轮未修改业务代码、正式数据或生产环境。
- 2026-08-25：第 1 项完成本地实现与回归。医生端为四步；设计服务按 D-208 使用通用资料、牙位和设计备注，不进入实体生产／物流，医生确认后完成数字流程，账单付款后开放原文件下载。前端构建与专项检查通过，后端目标测试 43 项通过，本地医生端浏览器只读验收四步与数字交付文案且无控制台错误；未提交订单、提交代码、推送、热更新或修改正式数据。
- 2026-08-25【历史错误口径，已由 D-213 撤销】：第 4 项曾按 D-211 把“推簧”作为 `ORTHO_SPRING_APPLIANCE / 弹簧矫正器` 产品检索别名并完成本地实现。重新核对后确认原表没有“推簧”，该同义关系证据不足；V92／V93、活动目录别名返回和医生端搜索不得提交或发布。
- 2026-08-25：用户指出设计服务制作要求缺项；复核同一指纹的原 DOCX 后按 D-212 恢复数据格式、设计标准、设计时间三项必选，备注继续选填。V94 覆盖全部既有设计服务目录版本，医生端字段、旧草稿兼容、缺项定位、最终复核和服务端允许值校验已同步；`OrderCaseGroupTests` 11 项、专项检查、i18n 与前端构建通过。
- 2026-08-25：按用户要求把《动态下单表最终版》12 页内容固化为来源快照。复核发现 D-211 把测试报告栏目位置当成同义关系证据，结论不足；按 D-213 将其重新打开。V92／V93 等错误别名实现未提交、未推送、未发布。
- 2026-08-25：按 D-215 完成螺旋扩弓方向本地修复。V95 为所有目录版本增加“单向／双向”必选规格并清理错误推簧别名；医生端显示“扩弓方向”，服务端拒绝空规格，订单快照冻结方向。后端 21 项、目录及医生下单专项、验收基线、i18n 和前端生产构建通过；浏览器只读确认产品分组与控制台，无验收订单写入。
- 2026-08-25：经 PR #55／#56 发布正式版本 `78d68ec17f7e0c1d7f625f0594cc21c6ab07773c`。自动部署 #32843001050 走 `full`，5m50s 内完成后端 362 项、release gates、双镜像、发布包、MySQL 备份、双容器替换、健康检查与公网探针；首页 200、无效登录 401、别名域名 308。未创建或修改正式订单。

## Remaining Work

- 本 Goal 本地实现范围已完成；“推簧”保持 `USER_DEFERRED`，不属于当前关闭条件。
- 提交、推送和正式发布已完成；后续只需按真实账号补充业务交互验收，不阻塞本 Goal 关闭。Task 8 保持 `NOT_READY`。
