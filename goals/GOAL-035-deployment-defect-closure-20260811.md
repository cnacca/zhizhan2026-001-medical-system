# GOAL-035 8088 部署缺陷与下单回归收口

Status: `completed`（本地代码与自动化收口，2026-08-11；线上重新部署与真实环境复测待执行）

Mode: `stage-goal`

## Summary

基于 2026-08-11 的线上 8088 联调报告和当前 `origin/dev` 基线，收口可在本地代码库中确认并修复的部署、文件访问和医生端下单缺陷。目标不是把“代码已修”冒充“线上已恢复”，而是先建立回归证据，再交付明确的重新部署与真实环境复测步骤。

## Source Baseline

- 代码基线：`origin/dev` @ `890ed1139c6e060557b4d6e650e47241a26ff7ff`。
- 联调材料：`2026-08-11_最新仓库与8088线上联调复测.zip`。
- 线上入口：`http://43.129.232.106:8088`。
- 已复现：登录请求带线上 Origin 时被 CORS 拒绝；`npm run check:task9d4` 因过期角色断言失败。

## Scope

- BUG-019：登录控制器统一消费 `APP_CORS_ALLOWED_ORIGIN`，并修正误导性前端报错。
- BUG-020：软删除文件不得再签发预览、下载或分片上传 URL，并保留拒绝审计。
- BUG-021：MinIO 内部访问地址与浏览器签名地址分离，正式 compose 显式暴露 API 端口并要求注入公网地址。
- BUG-022：更新 9D.4 静态校验，使其符合“角色决定入口、权限码决定业务动作”的现行模型。
- BUG-012 / BUG-013：医生端每一步离开前执行本步必填校验；草稿保存、下一步和最终提交共用互斥锁，避免连点并发版本冲突。
- BUG-015：验证已有固定表格布局和生产备注两行截断不回归。
- 生产前端不得预填或打包 `change-me-*` 演示密码。

## Non-goals

- 不在本地直接修改服务器环境变量、开放云防火墙、重启线上容器或改生产数据。
- 不声称本地测试通过等同于 8088 已恢复；线上状态必须在重新部署后复测。
- 不物理删除 MinIO 历史对象；本批阻止软删除文件生成新的签名 URL。已签发 URL 在 TTL 内的立即吊销需要对象移动/删除或改为后端代理下载，属于需确认的数据保留策略。
- 不提交真实数据库、MinIO、DeepSeek 或账号凭据。
- 不把 Task 8 改为 `READY`。

## Acceptance

- 配置的线上 Origin 可登录，未配置 Origin 仍返回 CORS 403。
- `status = 'DELETED'` 的文件对医生、客服和管理员都不能生成新预览/下载 URL，且记录拒绝审计。
- 签名 URL 使用 `MINIO_PUBLIC_ENDPOINT`，存储读写与 bucket 检查继续使用内部地址。
- 医生端缺必传文件时不能离开上传步骤；快速连点只会进入一个保存/跳步事务。
- `npm run check:task9d4`、本批机器检查、后端目标测试、前端生产构建全部通过，构建产物不含演示密码。
- BUG-015 的固定布局与两行截断静态回归通过。
- Task 8 保持 `NOT_READY`，线上复测与正式账号轮换列为待执行项。

## Risks

- `MINIO_PUBLIC_ENDPOINT` 必须是浏览器可达且与签名时 Host 一致的完整 origin；仅改返回字符串会破坏 AWS V4 签名。
- 若服务器只开放 8088 而未开放 MinIO API 端口或独立文件域名，BUG-021 仍无法在线上闭环。
- 数据库中的演示账号是否仍启用只能在真实部署环境核验；本批只保证生产前端不暴露默认密码。

## Outcome

- BUG-019 / 020 / 021 / 022 / 012 / 013 已完成本地修复，BUG-015 已纳入静态回归；生产构建不再预填或打包 `change-me-*` 演示密码。
- 后端全量回归在独立全新数据库与 MinIO bucket 上通过：336 tests，0 failures，0 errors；目标回归 27 tests 全绿。
- 前端生产构建、部署缺陷专项检查、9D.4、9D.69、部署环境、9D.78、compose 渲染、acceptance 和差异检查通过。
- 线上 `http://43.129.232.106:8088` 尚未使用本批代码与配置重新部署，因此本 Goal 的 completed 只代表本地交付闭环，不代表线上恢复或 Task 8 就绪。
