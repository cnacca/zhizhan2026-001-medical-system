# 管理端前端不可偏离实施规格

> 规格版本：`ADMIN-UI-BASELINE-V1.1`
> 冻结日期：2026-07-16
> 适用范围：管理端全局外壳、左侧导航、页面结构、交互、权限表达与前端验收
> 规格状态：`CONFIRMED_AND_FROZEN`；全局规则与人员管理 V2 已由用户确认，后续必须按本文件实施

## 1. 规格效力

本文件是管理端前端后续设计、开发、评审和验收的唯一总规格。文中的“必须”“不得”“仅”均为强制要求，不是建议。

执行优先级从高到低为：

1. 用户在对话中最新、明确确认的书面要求；
2. 本文件；
3. `docs/design/admin-portal/` 下的组件规格；
4. `docs/design/reference-portals/README.md` 中的三端视觉研究基线；
5. 现有代码行为。

现有代码、旧截图或旧说明与本文件冲突时，必须修改现有实现，不能以“代码已经存在”为理由保留冲突设计。除非用户再次明确书面确认，不得自行增加、删除、改名、合并或拆分菜单，不得改变权限层级、工作台边界和固定工艺链规则。

## 2. 产品定位与设计目标

管理端用于跨部门查看和处理平台运营事务，包括订单协同、生产运营、内部人员与权限、产品配置、审计通知和智能功能治理。它服务的是管理员及具有管理职责的内部人员，不是医生端、客服端或生产员工个人工作站的简单合集。

界面必须满足以下用户目标：

- 用户能在左侧一级菜单中直接找到业务入口，不通过多层折叠菜单寻找功能；
- 同一业务对象的相关能力放在同一入口内，使用页内切换、筛选、抽屉或弹窗完成；
- 用户先看到当前情况和待处理事项，再看到配置与技术细节；
- 所有文案使用业务中文，不显示英文角色码、接口名、数据库字段、阶段编号、测试状态码或调试信息；
- 展示和保存结果必须来自真实数据与真实接口，不复制参考 HTML 的演示数字，不伪造成功状态。

## 3. 左侧菜单——最终版本

左侧固定为 3 个分组、17 个一级入口。一级入口全部直接可点击，不再显示二级折叠菜单。

| 分组 | 顺序 | 一级入口 | 主路由 | 强制边界 |
| --- | ---: | --- | --- | --- |
| 业务协同 | 1 | 工作台 | `/dashboard` | 业务内容冻结，只统一外壳 |
| 业务协同 | 2 | 订单管理 | `/orders/internal` | 页内承载订单列表、文件资料 |
| 业务协同 | 3 | 沟通中心 | `/collaboration` | 页内承载消息处理、沟通管理 |
| 业务协同 | 4 | 客户管理 | `/admin/clinics` | 管理医生、诊所、联系人与偏好 |
| 业务协同 | 5 | 账单配送 | `/delivery` | 账单、发货、物流协同 |
| 业务协同 | 6 | 外协管理 | `/admin/outsourcing` | 独立入口和独立页面 |
| 生产运营 | 1 | 工艺生产 | `/workflow/process-instance` | 页内承载工序进度、员工派工；固定工艺链只读 |
| 生产运营 | 2 | 质量管理 | `/production/quality` | 质量汇总、外返、返工责任、终检状态 |
| 生产运营 | 3 | 人员管理 | `/admin/staff` | 账号、职责权限、组织岗位统一管理 |
| 生产运营 | 4 | 绩效统计 | `/performance` | 工时、绩效、返工归因 |
| 生产运营 | 5 | 设备管理 | `/production/devices` | 暂时保留独立入口，不改名、不拆分 |
| 生产运营 | 6 | 物料管理 | `/production/material-exceptions` | 暂时保留独立入口，不改名、不拆分 |
| 生产运营 | 7 | 安环管理 | `/production/safety-environment` | 暂时保留独立入口，不改名、不拆分 |
| 生产运营 | 8 | 成本管控 | `/production/cost-management` | 暂时保留独立入口，不改名、不拆分 |
| 系统治理 | 1 | 产品配置 | `/system/form-configs` | 页内承载动态表单、返工字典 |
| 系统治理 | 2 | 审计通知 | `/notifications` | 操作审计、通知、安全事件 |
| 系统治理 | 3 | AI 治理 | `/admin/ai-governance` | 使用用户语言表达智能功能与保护规则 |

以下入口不得重新出现：独立“账号权限”、独立“用户管理”、独立“角色权限”、独立“工序链查看”。个人账号卡片可以保留个人资料、账号切换和退出登录，但不得重复承载人员与角色管理。

## 4. 页内合并规则

| 一级入口 | 页内结构 | 不允许的做法 |
| --- | --- | --- |
| 订单管理 | `订单列表 / 文件资料` 两个页内标签 | 在侧栏增加“文件资料” |
| 沟通中心 | `消息处理 / 沟通管理` 两个页内标签 | 在侧栏增加“沟通管理” |
| 工艺生产 | `工序进度 / 员工派工` 两个页内标签；页头按钮打开固定工艺链抽屉 | 把“工艺链查看”做成菜单或可编辑页面 |
| 人员管理 | 单页人员名单；组织筛选、账号资料和职责权限在同一工作区完成 | 使用“用户管理 / 角色权限 / 组织岗位”三页签；恢复独立账号权限入口 |
| 产品配置 | `动态表单 / 返工字典` 两个页内标签 | 在侧栏展开两个子菜单 |

页内标签只用于同一业务入口中的主要视图切换。切换后左侧一级菜单必须保持选中，不新增侧栏层级。

## 5. 权限与数据范围——最终规则

内部人员层级固定为：`管理员 → 经理 → 主管 → 普通员工`。

| 当前人员 | 可分配的级别 | 可作用范围 | 禁止行为 |
| --- | --- | --- | --- |
| 管理员 | 经理、主管、普通员工 | 全公司 | 无法绕过审计或授予系统不存在的权限 |
| 经理 | 主管、普通员工 | 自己负责的部门或业务范围 | 分配管理员、经理；越过负责范围 |
| 主管 | 普通员工 | 本部门或本班组 | 分配管理员、经理、主管；跨部门或跨班组 |
| 普通员工 | 无 | 仅本人可见或被授权的数据 | 给任何人分配角色或权限 |

通用强制规则：

- 人员级别决定授权上限，具体菜单和操作仍由后端返回的权限集合决定；不得把经理或主管硬编码成一套无法调整的固定菜单；
- 管理员可见全部 17 个管理端入口；经理和主管只显示上级已授予且在本人数据范围内的入口；普通员工默认不进入管理端，也不显示任何授权动作；
- 经理或主管只有在被授予人员管理能力时才显示“人员管理”，进入后仍分别限定为本人负责范围或本部门/班组；
- 任何人不得授予同级或更高级角色；
- 任何人不得授予自己不拥有的权限；
- 任何人不得扩大到自身组织范围或数据范围之外；
- 菜单可见性、按钮可操作性和后端数据范围必须一致，不能只做前端隐藏；
- 所有人员、角色、组织归属和权限变更必须由后端校验并写入审计记录；
- 医生和诊所账号归入客户管理，不得混入内部人员管理；
- 当前后端尚未支持的经理/主管保存能力必须禁用并明确说明，不能显示虚假保存成功。启用前必须先完成后端权限与审计闭环。

## 6. 人员管理 V2——必须重新设计

### 6.1 旧稿处置

以下旧稿已废止，不得继续作为开发或验收依据：

- `docs/design/admin-portal/reference/personnel-management-1440x900.png`；
- `docs/design/admin-portal/reference/personnel-management-concept.html`；
- 旧稿中的四张顶部指标卡；
- “用户管理 / 角色权限 / 组织岗位”三页签；
- 常驻在列表上方的四级权限关系带。

旧文件可以暂时保留用于差异追溯，但必须标注“已废止”，不得在任务、PR 或验收报告中称为最终参考。

### 6.2 V2 用户任务顺序

页面按用户最常见的操作顺序组织：`找人 → 查看归属与职责 → 新增或编辑人员 → 设置可管理范围`。

默认页面由以下部分组成：

1. 页头：标题“人员管理”、一句简短说明、次按钮“部门与岗位”、主按钮“新增人员”；
2. 主工作卡：左侧为组织范围筛选，右侧为人员名单；
3. 左侧组织区：全部人员、管理人员、普通员工和真实部门列表，显示真实人数；
4. 右侧工具栏：一个搜索框、人员级别筛选、账号状态筛选、结果数量；
5. 人员表格：人员、部门/岗位、人员级别、管理范围、账号状态、当前任务、操作；
6. 行操作仅保留主要动作“查看 / 编辑”，更多低频动作进入抽屉；
7. 人员抽屉：在同一抽屉内依次呈现“账号资料、组织归属、职责权限、账号状态”；
8. 部门与岗位：从页头次按钮进入独立抽屉或弹窗，不回到三页签结构。

### 6.3 V2 简化原则

- 默认页不放经营型 KPI，不用四张大卡占据首屏；
- 默认页不向所有用户持续解释完整权限体系；只在查看或设置职责时显示与当前人员相关的可分配范围；
- 用户管理和角色权限不是两个页面，而是同一个人员记录的两个组成部分；
- 当前操作者只能看到自己有权管理的人和可分配选项；不可分配项不出现在可选列表中；
- “部门与岗位”是低频组织维护动作，不挤占日常人员名单；
- 1440×900 首屏至少显示 7 行人员数据；1280×800 至少显示 5 行；
- 没有最近登录数据时显示真实更新时间，不得伪造登录时间。

### 6.4 V2 状态与交互

- 搜索即时匹配姓名、账号、部门和岗位；人员级别与账号状态可组合筛选；
- 点击左侧组织项立即过滤右侧名单，并保持清晰选中态；
- 点击人员行或“查看 / 编辑”打开右侧抽屉；关闭按钮、遮罩和 `Esc` 均可关闭；
- 新增人员默认创建普通员工；是否允许调整级别由当前操作者权限与后端能力共同决定；
- 保存中禁止重复提交；保存成功后刷新该人员与列表；失败时使用可理解的中文说明；
- 空数据、加载中、请求失败、无权限必须在当前工作区显示，不跳到技术错误页；
- `/admin/staff/roles` 与 `/admin/staff/organization` 旧链接仅可兼容重定向到 `/admin/staff`，不得继续呈现旧三页签页面。

### 6.5 V2 视觉确认门

人员管理 V2 候选稿已经产出：

- 可交互参考：`docs/design/admin-portal/reference/personnel-management-v2-concept.html`；
- 固定截图：`docs/design/admin-portal/reference/personnel-management-v2-1440x900.png`；
- HTML SHA-256：`70024bad766d22339d83fd71c3baf523f0b744e7bc5070cfb72897efbeb941e0`；
- 截图 SHA-256：`6bc0d0be582c33d31fc69347c4fd659dd113a29d12722b30946202ae399a39b4`。

2026-07-16 已使用本机真实 Chrome 在 `1440×900` 视口验证：页面尺寸无溢出，组织筛选、搜索、组合筛选、人员抽屉、部门岗位弹窗和 `Esc` 关闭可用。同日用户已明确确认该 V2 参考稿；后续人员页面必须据此实现，不得恢复旧稿结构。

## 7. 固定 9 条工艺链

- 9 条工艺链是既定流程，不是日常配置项；
- 9 条链固定为：常规冠修复、种植类修复、精密附件、套筒冠、贴面修复、活动件-钢托、活动件-胶托、活动件-隐形、正畸；
- “查看固定工艺链”位于工艺生产页头，打开右侧只读抽屉；
- 抽屉左侧按既定顺序列出 9 条工艺链，右侧显示节点顺序、可选节点和并行分组；
- 不得出现新增、编辑、删除、复制、拖拽排序、保存或发布按钮；
- 工序进度展示订单运行到哪一步；固定工艺链展示标准流程是什么，两者不得混为同一概念。

## 8. 其他页面的固定内容边界

| 页面 | 首屏核心内容 | 主要交互 |
| --- | --- | --- |
| 订单管理 | 订单状态、客户、产品、进度、异常、更新时间 | 搜索、筛选、查看订单抽屉、进入文件资料 |
| 沟通中心 | 待处理消息、订单上下文、发送方、等待时长 | 筛选、查看上下文、处理消息 |
| 客户管理 | 诊所、联系人、偏好、账号状态 | 搜索、查看/编辑客户抽屉 |
| 账单配送 | 账单状态、发货状态、物流异常 | 筛选、查看账单与物流详情 |
| 外协管理 | 外协事项、供应商、金额、状态、交期、说明 | 新增外协记录、查看/更新状态 |
| 工艺生产 | 订单工序进度、节点状态、执行人 | 查看固定链、派工、查看节点详情 |
| 质量管理 | 质量汇总、返工、外返、终检 | 筛选、查看记录、处理状态 |
| 绩效统计 | 工时、完成量、质量与返工归因 | 时间/部门/人员筛选、查看详情 |
| 设备管理 | 设备台账、运行状态、维护事件 | 保留现有基础入口，不扩成完整设备闭环 |
| 物料管理 | 物料异常、处理状态 | 保留现有基础入口，不扩成完整库存系统 |
| 安环管理 | 巡检、隐患、整改状态 | 保留现有基础入口，不扩成完整安环审批 |
| 成本管控 | 工序、材料、人工、返工、外协成本与预警 | 保留现有基础入口，不扩成财务结算系统 |
| 产品配置 | 动态表单、返工原因与责任类型 | 页内切换、按现有真实能力维护 |
| 审计通知 | 关键操作、通知、安全事件 | 筛选、查看详情 |
| AI 治理 | 智能功能、安全保护、预算保护、人工确认 | 只读查看当前规则与状态 |

外协管理必须使用独立 `/admin/outsourcing` 导航状态和页面，不得与成本管控共用页面。设备管理、物料管理、安环管理、成本管控本轮只做入口和现有基础能力的视觉统一，不得擅自扩大业务范围。

## 9. 全局视觉规格

管理端吸收三个参考端共同的骨架与组件语言，但使用独立管理蓝，不直接复制医生蓝、客服紫或生产青。

| 项目 | 固定值或规则 |
| --- | --- |
| 桌面基准 | `1440×900` |
| 次级验收 | `1280×800` |
| 侧栏 | `230px`，`#0f172a`，固定深色纹理背景 |
| 顶栏 | `56px`，白色吸顶 |
| 页面背景 | `#f7f9fc` |
| 主强调色 | `#2563eb` |
| 主文字 | `#0f172a` |
| 次文字 | `#64748b` |
| 卡片 | 白色，`14px` 圆角，`1.5px solid #e2e8f0` |
| 控件 | 以 `8px` 圆角为主，必须有 hover、focus-visible、disabled 状态 |
| 字体 | `Plus Jakarta Sans` + `PingFang SC` / `Microsoft YaHei` / 系统字体回退 |
| 图标 | 同一套内联线性 SVG；不得混用 Emoji、图标字体英文兜底和风格不一致的第三方图标 |

所有管理端样式必须限定在管理端作用域，不能改变医生端、客服端和生产端。移动端当前只要求不崩坏；不能把自行增加的移动布局宣称为参考 HTML 的像素级复刻。

### 9.1 页面顶部唯一设计基线

2026-07-16 用户再次明确确认：管理端所有页面只在“页面顶部”层面以人员管理为唯一设计基线。这里的页面顶部仅包括：

1. 56px 管理端全局顶栏；
2. 面包屑、全局搜索、帮助和审计通知入口；
3. 页面眉题、一级标题、说明文案和右侧页面级操作按钮；
4. 页面存在内部主要视图时，紧接标题区下方的页内标签栏。

人员管理的主体布局不是其他管理页面的通用模板。不得因为顶部统一，就把人员管理的“左侧人员范围 + 右侧人员名单”、人员表格字段、人员抽屉或组织筛选结构复制到订单、沟通、客户、账单配送、生产或系统治理页面。

各页面主体必须根据本文件第 8 节的业务边界和三个参考 HTML 建立独立映射，再结合真实接口完整重构。订单管理的两行筛选是订单页专属设计，不属于可推广的全局组件基线。

## 10. 工作台冻结规则

- 不修改工作台业务内容、卡片结构、图表、指标、数据口径和操作；
- 允许工作台跟随管理端统一侧栏、顶栏、背景和内容容器；
- 为统一全局外壳所做的最小 CSS 适配不得改变工作台信息层级；
- 验收时必须保留改造前后同视口截图，确认没有非必要变化。

## 11. 交互与数据真实性

- 表格行详情优先使用右侧抽屉；编辑或确认使用抽屉/弹窗，不跳出管理端外壳；
- 所有抽屉支持关闭按钮、遮罩和 `Esc`；所有弹窗具有取消、确认、保存中和失败状态；
- 加载、空数据、请求失败、无权访问均使用当前页面内的中文状态；
- 宽表只能在内容卡内部横向滚动，`1280×800` 不得出现页面级横向滚动；
- 全局搜索默认进入订单列表，按订单号、诊所或人员关键字查询；
- 参考 HTML 中的演示账号、演示角色切换、模拟数字和 `localStorage` 联动不得进入正式实现；
- 文件预览和下载继续使用真实鉴权与短时效地址，不得为复刻效果绕过权限。

## 12. 实施顺序

1. 冻结本文件并校验菜单、路由和当前代码差异；
2. 按用户已确认的人员管理 V2 参考稿替换旧实现；
3. 统一管理端外壳与共用组件；
4. 按业务协同、生产运营、系统治理顺序逐入口实施；
5. 每完成一个入口即接真实数据、补状态和真实浏览器路径；
6. 最后执行全菜单、全页内切换、权限、响应式和工作台回归。

不得把旧人员页结构扩展到其他页面；不得以一次性大改替代逐入口验收。

## 13. 验收定义

管理端前端只有同时满足以下条件，才能声明本轮完成：

- 3 组 17 个一级入口名称、顺序、路由和选中态全部符合本文件；
- 订单、沟通、工艺、产品页内切换正确；人员页符合已确认的 V2 参考；
- 权限显示、可操作按钮、后端校验与数据范围一致；
- 固定 9 条工艺链只读，外协为独立页面，四个保留入口未被擅自改造；
- 工作台业务内容无非必要变化；
- 在真实 Chrome 中完成全部入口点击、关键筛选、抽屉、弹窗、保存/失败路径；
- `1440×900` 与 `1280×800` 截图通过，控制台无新增错误，无未解释失败请求；
- lint、typecheck、build 和相关自动化测试通过；
- 验收报告明确列出真实业务差异、尚未具备的后端能力和未覆盖项；
- 没有用占位页、假数据、假成功、权限绕过或仅构建通过冒充完成。

## 14. 变更控制

任何后续变更必须记录：变更原因、受影响菜单/页面/权限、用户明确确认内容、新规格版本和回归范围。没有用户明确确认时，本文件保持有效，实施者不得凭个人偏好偏离。

### V1.1 变更记录（2026-07-16）

- 用户确认：只将人员管理页面顶部作为后续管理页的统一设计基线；
- 用户确认：订单两行筛选仅属于订单页，不是其他页面的通用模板；
- 用户确认：剩余管理页面需要完整重构页面主体，不是只统一顶部；
- 抽屉宽度和订单详情结构不因本次顶部统一而调整；
- 工作台业务内容继续冻结，只允许跟随全局外壳。

本规格明确覆盖以下旧口径：旧文档中“Manager 与 Admin 并列最高权限”的表述、允许外协复用成本页的表述、独立账号权限菜单、可编辑工艺链和人员管理三页签。旧测试若仍断言这些行为，必须更新旧测试，不得反向修改本规格。
