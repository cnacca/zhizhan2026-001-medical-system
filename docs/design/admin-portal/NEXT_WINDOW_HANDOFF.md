# 管理端剩余页面重构下一窗口交接

> 日期：2026-07-16
> 状态：`ADMIN_REMAINING_PAGE_DESIGNS_CONFIRMED`

> 2026-07-16 最新确认：只将人员管理页面顶部作为统一设计基线；剩余管理页面主体需要逐页完整重构。详细方案见 `docs/design/admin-portal/REMAINING_PAGES_REFACTOR_PLAN.md`。

## 新窗口第一读取顺序

1. `docs/design/admin-portal/IMPLEMENTATION_BASELINE.md`
2. `docs/design/admin-portal/REMAINING_PAGES_REFACTOR_PLAN.md`
3. `docs/quality/admin-order-page/README.md`
4. `docs/design/reference-portals/README.md`
5. `git status --short --branch` 与当前未提交差异

## 已确认结论

- 管理端总规格为 `ADMIN-UI-BASELINE-V1.18`，状态 `CONFIRMED_AND_FROZEN`；
- 左侧固定 3 组 17 个一级入口；
- 人员管理 V2 已由用户确认；只有其页面顶部是其他管理页面的统一视觉基线；
- 人员页固定为“左侧组织筛选 + 右侧人员名单 + 单一人员/权限抽屉”；
- 不恢复四张人员指标卡、人员三页签、独立账号权限或常驻权限关系带；
- 权限层级为管理员 → 经理 → 主管 → 普通员工，并受后端权限和数据范围约束；
- 固定 9 条工艺链继续作为系统模板，但管理端不提供独立查看或编辑入口；
- 设备管理、物料管理、安环管理、成本管控保留独立入口；
- 外协管理使用独立页面；
- 工作台业务内容不修改。
- 订单两行筛选是订单页专属设计，不得复制到其他页面；
- 剩余管理页面需要完整重构主体，不是只换顶部。

## 当前订单页状态

当前订单顶部和两行筛选已经写入 `frontend/src/App.vue` 与 `frontend/src/admin-order-page.css`。前端构建和 `git diff --check` 已通过，真实 Chrome 1440×900 已完成；截图为 `docs/quality/admin-order-page/screenshots/final-order-top-personnel-style-1440x900.png`。

2026-07-16 终验已补齐：真实 Chrome 管理员登录后在 `1280×800` 验证订单列表仅在内容卡内部横向滚动、文件资料页签正确显示真实空状态，刷新接口 `GET /orders?page=1&size=100` 返回 `200 OK`，并已新增截图和记录到 `docs/quality/admin-order-page/README.md`。控制台没有应用业务错误；仅有 Vite 开发热更新 WebSocket 连接失败日志，不影响订单 REST 请求或页面交互。`npm run build:frontend` 与 `git diff --check` 通过。订单表格、文件资料主体和 820px 详情抽屉保持原实现，未调整抽屉宽度。

## 当前沟通中心状态

2026-07-16 已按客服端 Comms Centre、生产端消息审核提示和客服端资料处理队列完成管理端“消息处理 / 沟通管理”主体重构。用户要求先补齐严格映射缺口后，本轮进一步完成：固定 290px 线程栏、真实角色快捷筛选、待处理状态点、智能状态缺失说明、附件能力禁用说明、发送箭头、真实审核提示、六列处理队列和 560px 订单沟通上下文抽屉。后端未提供的时间、责任人、AI 状态和附件能力均明确显示“暂未提供”或安全禁用，没有写入演示数据。

真实 Chrome 已在 `1440×900`、`1280×800` 覆盖两个页签；整页无横向溢出。选择真实订单后，`GET /orders/10551/messages` 与 `GET /orders/10551/message-mentionable-users` 返回 `200 OK`，560px 抽屉无内部横向溢出；刷新 `GET /messages/pending-review` 返回 `200 OK`，控制台无错误或警告。当前本地环境为 0 条待审核消息，本轮未发送、审核或修改真实消息。最终截图和完整区域 / 视觉资产映射见 `docs/quality/admin-communication-page/README.md`。用户已于 2026-07-16 明确确认该版本并允许进入客户管理。

## 当前客户管理状态

用户已确认新的目标设计：客户管理采用“客户目录 / 客户贡献”两个页内页签，默认客户目录；客户目录使用高密度诊所表格和 560px 只读详情，客户贡献次级页签复用现有平台接单/出货金额背景和本月客户订单/件数排名。客户级接单/出货金额保留位置，在后端聚合补齐前显示“暂未统计”，不得以前端不完整计算代替。

用户要求先完成所有剩余页面的设计讨论并写入文档，再统一进入执行。因此当前客户页代码仍是已被新方案取代的旧实现，旧截图只保留历史证据，不代表新方案验收通过。刚才误开始的临时页签/销售条代码已撤回，前端构建和 `git diff --check` 通过；本轮没有新增后端改动。

## 当前账单配送设计状态

用户已确认账单配送保持一个页面，并采用两层页内切换：第一层“国内业务 / 国外业务”，第二层“账单与收款 / 配送跟踪”，默认进入“国内业务 / 账单与收款”。国内和国外均可查看已发货订单状态；分区按订单最终配送目的地判断。

当前设计只展示系统人工维护的承运商、运单号、已发货和异常跟进状态，不把它描述为承运商实时轨迹。账单与配送共用 560px 详情；管理端线框以查看监督为主，客服端继续执行账单上传、人工收款登记和物流录入。设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.5，尚未编码。

## 当前外协管理设计状态

用户确认外协管理按订单内的外协件或外协批次建记录，同一订单允许多条记录分别追踪供应商、发出时间、预计返回、实际返回、超时和异常。订单管理增加外协订单筛选、外协状态和详情跳转；外协金额与供应商费用归成本管控。

参考 `factory #page-datacenter` 只复用外协跟踪表和时间线，不复制合作站点、云端文件、外部请求、聊天或实时网络。当前后端只有外协成本事实，缺少外协件/批次履约模型；设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.6，尚未编码。

## 当前工艺生产设计状态

用户确认工艺生产采用一个页面框架：主表按订单展示实际工序进度、当前工序、执行人、时限和异常；点击订单进入 560px 工序详情，在具体工序节点上完成员工分配或转派。员工派工不是独立页签或第二套任务列表。

管理端不代替生产员工开始、完成或跳过工序。固定工艺链不设独立查看入口，订单详情只显示实际采用的链名称、分支和真实节点时间线。设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.7，尚未编码。

## 当前质量管理设计状态

用户确认质量管理采用“监督 + 最终确认”：生产端执行入检、出检和返工，客服端登记外返，管理端查看质量汇总、确认责任、监督处理并完成最终关闭。管理端不代替生产人员检查，也不重新录入客服侧外返。

页面使用一套质量问题框架：顶部为真实质量指标带，主体合并展示内返和外返问题，560px 详情显示检查记录、返工影响、责任归属、处理记录和终检结果。终检报告只在关联订单详情中按真实能力查看或生成，不新增全局报告列表。设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.8，尚未编码。

## 当前绩效统计设计状态

用户确认绩效统计以员工横向对比为主，以 560px 个人绩效详情为辅。主表按统一时间范围展示员工完成量、工时、标准覆盖率、准时率、通过率、工时效率、返工归因和后端绩效参考分；详情展示指标口径和订单/工序级工时明细。

当前接口只支持单员工统计与明细，没有全员或部门聚合。不修改后端时，前端只能对当前可见人员分页、限并发查询，并保留单行加载、空和失败状态。页面不复制预计工资、计件单价、工资表导出或演示薪酬算法。设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.10，尚未编码。

## 当前设备管理设计状态

用户确认设备管理包含设备清单，以及状态、维护、校准、维修和报废登记。生产人员或设备负责人登记状态/维护/校准并发起维修或报废，经理或管理员执行单级审批；维修通过后登记维修结果，报废通过后设备锁定为已报废。

页面保留一个左侧入口，页内为“设备清单 / 审批事项”。设备详情使用统一时间线承载各类历史，审批事项集中处理维修和报废。当前后端缺少设备列表、详情、分类历史和审批接口，设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.11，未经授权暂不编码。

## 当前物料管理设计状态

用户确认当前没有明确库存需求，物料管理严格限定为物料异常管理。页面只处理缺料、错料、批次异常和材料损耗，以及关联订单/工序、责任和处理状态；不增加物料清单、库存、采购、入出库、领用、仓位和盘点。

页面使用一张异常队列和 560px 详情。当前后端只有汇总、登记异常和按异常编号更新状态，缺少异常列表与详情接口；设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.12，未经授权暂不编码。

## 当前安环管理设计状态

用户确认安环管理按部门设置安全检查和环境检查规则，并使用固定日历周期生成检查任务。到期未检查自动显示逾期未检，检查异常自动进入整改；部门负责人或指定人员执行检查和整改，管理端维护规则、监督执行并复核关闭。

页面使用“检查监督 / 检查规则”两个页内标签，周期任务、临时隐患和 PPE/设备提醒进入同一监督队列。当前后端缺少规则、任务生成、自动逾期、检查结果和整改复核模型，设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.13，未经授权暂不编码。

## 当前尺寸设计状态

用户确认所有页面结构必须带尺寸。统一尺寸合同已经写入 `IMPLEMENTATION_BASELINE.md` 9.1，已确认页面的纵向栈、主体卡片高度、行高、首屏行数、最小表宽、560px 抽屉和滚动边界已经回填到 `PAGE_COMPOSITION_MAPPING.md` 3.2.1。后续页面尺寸未确认前不得编码。

## 当前成本管控设计状态

根据客户原话，用户确认成本管控以人员、材料等分类成本监控和异常提示为主；订单/工序追溯降为详情依据。页面使用 72px 紧凑指标带和成本记录主表，`WARNING` 记录使用醒目状态与行标记，560px 详情追溯部门/供应商、订单、工序和异常说明。

成本页尺寸为 646/546px 主体卡片、44px 行高、1080px 最小表宽和 560px 抽屉。当前后端缺少成本列表、详情、确认、多维聚合和自动异常规则，金额记录也没有币种字段；第一阶段只认后端 `WARNING`，前端不自设阈值。设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.14，未经授权暂不编码。

## 当前产品总览设计状态

用户根据客户原话确认：产品配置权限归客服端，管理端“产品配置”正式改名为“产品总览”。管理端采用单页只读产品列表和 560px 详情，只显示产品资料、基础价格、币种、状态、备注和时间，不显示必填/选填、下单字段技术信息、编辑或模板导入。

返工原因与责任不属于产品配置，移入质量管理标题区的管理员设置入口。当前产品和动态表单后端权限与客户目标不一致，且没有模板导入接口；设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.15，未经授权暂不修改后端或客服端。

## 当前通知中心设计状态

用户确认不做审计中心，管理端“审计通知”正式改名为“通知中心”。页面只展示当前账号真实业务通知、关联订单、未读状态和时间，支持全部/未读、刷新、单条已读、全部已读和实时更新；不展示或预留操作审计、安全事件和日志查询。

通知中心采用 732/632px 主体卡片、56px 行高、1000px 最小表宽，不设置详情抽屉。设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.16，尚未编码。

## 当前智能服务设计状态

用户确认管理端不展示五项智能能力，也不把后端业务能力接口做成管理端功能目录。原“AI 治理”改名为“智能服务”，采用无页签单页结构，只帮助管理者查看全平台智能服务运行、风险、预计费用和预算使用情况。

页面顶部为 72px 标题，间隔 14px 后进入 732/632px 主体卡片；卡片内先展示 72px 的 24 小时成功调用、安全拦截、服务异常和预计费用/每日预算汇总，下方按约 2:1 展示近 7 日全平台调用/成本趋势，以及风险和预算状态。页面不设置抽屉。

当前 `/ai/governance/summary` 和 `/ai/governance/cost-trend` 只提供全平台汇总与趋势，不能按智能能力拆分。前端不得自行推算单项用量，不显示助手名称、外部提醒技术队列、模型、Prompt、密钥、Webhook、内部任务编号或 Task 8 状态。设计已写入 `PAGE_COMPOSITION_MAPPING.md` 5.17，尚未编码，未修改后端。

## 下一窗口首个实施任务

1. 客户管理目标设计已确认并写入 `PAGE_COMPOSITION_MAPPING.md`，暂不编码；
2. 账单配送、外协管理、工艺生产、质量管理、绩效统计、设备管理、物料管理、安环管理、成本管控、产品总览、通知中心和智能服务目标设计均已确认；
3. 全部剩余页面设计讨论已经完成，等待用户明确要求后，再从客户管理开始按逐页验收门禁进入实施。

## 工作树注意事项

当前主工作树路径为 `/Users/yuri/Documents/AI智能下单平台`，当前分支名为 `feature/project-skeleton`。工作区已有未提交修改与未跟踪设计文件；不得重置、覆盖或清理这些改动，必须先检查差异并在其上继续。不得修改工作台业务内容，不得把本地视觉验收描述为正式上线完成。
