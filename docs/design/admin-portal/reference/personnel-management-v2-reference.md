# 管理端人员管理 V2 候选参考说明

> 状态：`CONFIRMED_AND_FROZEN`
> 日期：2026-07-16
> 视口：`1440×900`
> 用户确认：2026-07-16，确认作为后续人员管理页面的最终视觉与交互参考

## 设计变化

V2 按“找人 → 查看归属与职责 → 新增或编辑 → 设置管理范围”的高频任务顺序重新组织人员管理：

- 删除旧稿四张顶部指标卡；
- 删除“用户管理 / 角色权限 / 组织岗位”三页签；
- 删除常驻权限层级关系带；
- 改为左侧人员范围与部门筛选、右侧人员名单；
- 账号资料、组织归属、职责权限和账号状态合并在同一人员抽屉；
- “部门与岗位”作为页头低频入口，使用弹窗处理；
- 默认 `1440×900` 首屏显示 7 行人员。

## 固定文件

- 可交互 HTML：`personnel-management-v2-concept.html`
- 固定截图：`personnel-management-v2-1440x900.png`
- HTML SHA-256：`70024bad766d22339d83fd71c3baf523f0b744e7bc5070cfb72897efbeb941e0`
- 截图 SHA-256：`6bc0d0be582c33d31fc69347c4fd659dd113a29d12722b30946202ae399a39b4`

## 真实 Chrome 验证

2026-07-16 已在本机 Chrome、`1440×900` 视口完成以下检查：

- 文档视口、滚动宽高均为 `1440×900`，无页面级横向或纵向溢出；
- 点击 CAD 部后名单显示 2 人；
- 搜索“王芳”后名单显示 1 人；
- 人员抽屉可以打开，`Esc` 可以关闭；
- 部门与岗位弹窗可以打开，`Esc` 可以关闭；
- 固定截图为真实 PNG，尺寸 `1440×900`。

另在 `1280×800` 视口复查：文档滚动宽高保持 `1280×800`，人员工作卡与分页完整可见，首屏 7 行均未被裁切。该次复查不作为新增固定截图，只用于验证次级桌面尺寸可用性。

候选页中的姓名、数量和保存提示均为设计演示数据，只用于展示状态。正式实现必须使用真实接口，且在经理/主管权限后端保存能力完成前不得伪造成功。

## 确认结论

用户已确认本稿。当前 `App.vue` 中的旧人员页和旧实现截图仍不是最终结果；下一执行窗口必须按本稿替换旧实现，并重新生成真实业务页面的验收截图。
