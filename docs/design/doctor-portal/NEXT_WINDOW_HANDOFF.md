# 医生端 V2 前端实施与后端接入交接

更新时间：2026-07-20

## 1. 本轮结论

医生端 V2 已按 `frontend/public/reference/doctor-portal.html` 完成登录后非抽屉页面的参考级重构，并选择性合并到主工作树。登录页不在本轮范围；主工作树已有的订单、患者、通知和物流抽屉实现独立保留，未被本轮参考样式覆盖。

- 固定壳层：左侧菜单 `224px`、全局顶栏 `58px`、基准字号 `14px`、正文色 `#0f172a`、主体左右边距 `26px`。
- 工作台保留六项概览、需要处理、即将送达 / 到期、近六周趋势，并补齐配送四步追踪和延期提醒。医生端不展示内部返工、绩效或生产人员数据。
- 二期完整交互在 `?doctorMock=1` 下可演示；真实 API 模式只使用已存在的安全接口，未开放能力不伪造成真实数据。
- 旧的 `docs/quality/doctor-portal-pixel-clone/` 是上一轮工作树与接口证据，本轮不覆盖。

## 2. 页面与交互结构

| 页面 | 本轮结构 | 关键口径 |
| --- | --- | --- |
| 工作台 | 六项概览、需要处理、即将送达 / 到期、近六周趋势 | 只统一壳层，不加入内部生产指标 |
| 订单管理 | 搜索、状态 / 产品筛选、六项快捷条件、医生 / 标签 / 日期高级筛选、导出、分页 | 增加医生、诊所、标签、创建 / 到期；订单页不显示物流公司、承运商或运单号 |
| 新建订单 | 固定、种植、活动、正畸、隐形矫治、数字化设计六类目录 + 全屏六步 | 产品字段和可选确认项由后台配置驱动；32 牙位图支持修复 / 缺失状态；上传支持选择和拖放 |
| 订单详情抽屉 | 公开进度、订单资料、医生可见文件、确认记录、进入订单沟通 | 不展示内部工序、员工、质检、工时、返工、绩效或责任分类；收货待办只提供前往物流页入口 |
| 订单助手 | 医生公开信息查询、结果订单跳转、安全拒答 | 不包含 AI 翻译；不查询内部生产信息 |
| 患者管理 | 搜索、全部 / 治疗中 / 待复诊 / 已归档筛选、治疗摘要、新建 | 状态与更新时间仅由医生可见订单摘要派生；不显示手机号、完整生日或内部病史 |
| 账单与物流 | 五项 KPI、账期提示、状态筛选、按单 / 月结 / 发票退款 / 物流四标签、CSV 与 PDF 下载 | 按单订单付款后发货；物流和运单号仅在这里展示；送达后需医生确认收货 |
| 消息中心 | 330px 会话列表、搜索、全部 / 未读 / 已读、右键标未读、快捷回复 | 提供翻译能力标识但不伪造 AI 翻译结果；发送者统一为“订单服务”；不加入 @内部角色 |
| 设计 / 照片确认 | 消息卡片和订单抽屉共用多版本确认结构 | 医生可同意或驳回并留言；驳回意见必填；允许多轮新版本；最后同意后才继续制作 |
| 账户设置 | 横向账户 / 成员 / 通知 / 安全标签，诊所选择卡、账单配送字段和资质入口 | 未接入的诊所新增与资质上传会明确提示，不伪造保存；诊所管理员不能授予平台其他端权限 |
| 通知中心 | 420px 抽屉、搜索、全部 / 未读 / 已读、全部已读 | 与消息中心分离；不提供删除；点击按目标进入订单、消息或账单 |

## 3. 角色与权限口径

- 多角色用户登录后必须选择当前诊所身份；本轮顶部身份选择器已实现。
- 切换身份后重新加载该角色的数据与动作范围，权限不叠加。
- 医生默认 `SELF`；诊所管理员与前台默认 `CLINIC`；护士默认 `ASSIGNED`。
- 只有医生身份且订单返回 `APPROVE_REVIEW` / `REJECT_REVIEW` 动作时，才能操作确认。
- 账单权限和物流权限独立；只有诊所管理员可维护诊所成员。

前端已在请求中携带 `X-Clinic-Role`，用于后端补齐当前身份隔离。后端必须签发或解析与当前入口 / 当前诊所角色一致的身份上下文，不能继续使用多角色权限并集。

## 4. 前端实现文件

- `frontend/src/doctor/DoctorPortalV2.vue`：医生端壳层、所有页面、抽屉、弹窗及六步流程。
- `frontend/src/doctor/DoctorDynamicFields.vue`：后台产品配置驱动的动态字段。
- `frontend/src/doctor/doctor-portal-v2.css`：医生端 V2 独立视觉样式。
- `frontend/src/doctor/types/contracts.ts`：冻结的医生端 UI / Gateway 数据合同。
- `frontend/src/doctor/services/mockDoctorGateway.ts`：二期完整前端演示与交互闭环。
- `frontend/src/doctor/services/httpDoctorGateway.ts`：现有真实接口安全适配、分片上传、角色请求头和内部字段拒绝。
- `frontend/src/doctor/services/doctorGateway.ts`：Mock / API 数据源切换。
- `scripts/check-doctor-portal-v2.mjs`：V2 专项静态门禁。

## 5. 本轮浏览器验收

独立工作树验收环境：本地 Vite `?doctorMock=1`，视口 `1440 × 900` 与 `1280 × 800`；合并后主工作树已重新通过专项静态门禁与正式构建。

已完成的真人式点击路径：

1. 医生入口登录；本轮不修改登录页。
2. 工作台确认六项概览、双栏待办 / 交付、配送四步条和趋势区；确认 `224 / 58 / 14` 壳层基线。
3. 订单高级筛选、六项快捷筛选和字段列验证；确认订单列表无物流公司、承运商、运单号。
4. 六类产品目录逐类切换；活动修复自动进入缺失牙位模式，32 个牙位按钮可操作。
5. 全屏六步下单保留患者、动态字段、拖放上传、三类确认项和复核提交合同。
6. 提交后重新打开新订单详情；验证 STL 文件和三类确认记录均存在。
7. 设计稿多版本预览、同意二次确认、驳回必填门禁；同意后订单列表待办同步清除。
8. 消息搜索覆盖历史正文，全部 / 未读 / 已读筛选，发送消息。
9. 患者新建、列表出现、详情抽屉再次打开。
10. 账单四标签、物流详情、确认收货二次确认。
11. 多角色切换：诊所管理员不显示订单助手、新建订单和医生确认动作；切回医生后恢复。
12. 全局搜索、通知搜索和已读 / 未读筛选、订单助手安全拒答。
13. 中英文壳层切换、帮助与支持入口、发票 PDF 下载完成点击验证。
14. 干净页面复查为 `console error=0`、`warning=0`，且 `1280 × 800` 下无横向滚动。

本轮浏览器验收中发现并已修复：

- 消息搜索后右侧仍停留在旧会话。
- 新订单提交后能出现在列表，但详情不存在。
- 设计稿确认后列表仍显示“待确认”。
- 六步流程草稿保存失败仍会进入下一步。
- “搜索患者”输入框没有筛选作用。
- 文件只生成本地 ID，真实提交时会被过滤掉。
- 新建患者只改本地列表，随后无法打开详情。
- 角色切换只改变按钮外观，没有重新加载角色范围。

## 6. 后端待补合同

页面组件不需要重构；后端补齐时应以 `DoctorGateway` 合同为准，完成 API 适配：

- 当前诊所角色激活、角色专属 Token 或等价服务端身份上下文；实现 `SELF / CLINIC / ASSIGNED` 数据范围。
- 医生安全订单列表和详情投影，服务端白名单剔除内部字段。
- 医生可见产品目录、价格、动态表单字段和三类 `review_capabilities`。
- 通用三类确认接口：多版本、状态版本、幂等键、同意 / 驳回、驳回意见必填。
- 订单线程、历史消息搜索、会话已读持久化。
- 按单账单、月结账单、发票、退款、物流、确认收货。
- 诊所成员与角色、账单权限、物流权限、当前身份切换。
- 医生通知安全投影，按当前角色 / 入口过滤 audience；不能只按用户 ID 返回全部通知。
- 订单提交保存 `product_id`、`review_options` 和已经上传完成的 `file_ids`。

当前前端的第二道防线会递归拒绝含内部字段的订单响应，并过滤疑似内部通知；这不能替代服务端安全投影。多角色 Token 隔离和通知 audience 隔离仍是上线阻塞项。

## 7. 验证命令与已知旧检查

通过：

```bash
cd frontend && pnpm build
npm run check:doctor-portal-v2
npm run check:task9d24
npm run check:task9d61
```

部分旧 9D 检查仍会失败，因为它们用字符串包含判断要求已经从新版 `acceptance.json` / gap matrix 中移除的历史哨兵文本，或仍要求旧名称“医生 AI”和旧 `App.vue` 布局。本轮没有通过补假标记让旧检查变绿；以 V2 专项门禁、类型构建和真实浏览器验收作为当前证据。

## 8. 上线边界

- 当前是二期前端完成状态，不代表后端二期合同已完成。
- 未验证真实支付、真实物流平台、真实邮件 / 短信、真实多角色 Token 隔离和真实 P2 审核接口。
- `Task 8` 继续保持 `NOT_READY`，直到真实外部环境、权限隔离、医生安全投影和上述后端合同全部验收通过。
