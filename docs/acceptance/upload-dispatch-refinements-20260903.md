# 上传、派工与工作台补充验收（2026-09-03）

用户授权：当前方案已确认，可以执行。分支 `codex/fix-upload-dispatch-review`；保留 D-217 既有未提交改动。本记录不代表发布、客户逐项签字或一期整体完成。

## 实施口径

- 来源：原文件 SHA-256 不变。修正来源快照“逐字转录”的过度声明；上传章节实际在 Word 第 9–11 页。种植修复原文为 CBCT Imaging (complex)，导板限定在设计服务段。保留旧摘要的纠正记录，不改原文件。原始必传、解释和 D-198 / D-201 / D-217 后续确认分层。
- 上传：共享口扫、横向产品切换、当前产品资料、按需展开的专属口扫。共享附加资料标注适用产品；隐形矫治无比色照片，弹性义齿无侧面照；常规正畸保留标准五张口内照，不再重复显示通用口内照入口；固定修复恢复旧义齿参考；活动义齿颌位记录单独选传。CBCT 提供中文解释，保留现有种植/设计服务选传入口，不凭推断新增子产品医学要求。
- 不删除历史附件，不改变共享继承和专属覆盖，不增加提交门禁：仍为完整扫描包或上颌+下颌+咬合。照片、其他影像、颌位记录均选传。上下颌分类仍可人工调整。上传文件名可直接预览，预览弹窗层级覆盖向导。
- 派工：未分配才可“安排员工”；已分配只能“调整员工”，同人调整及终态工序禁用。409 保留后端冲突保护，前端刷新工序并给出可理解提示；候选人员范围未放宽。
- 检验：菜单改为“入检/出检登记”，说明人工登记、开工前入检和完工后出检场景。界面、导航和操作使用 `check:gate-inspect` 权限；全功能测试角色保留入口，不采用用户名绕过。
- 四端：下半部分统计卡片、客户排名、部门行、图中数据点可进入相应业务页面；订单/件数按月份筛选，客户排名叠加客户 ID，部门跳转工序队列保留部门范围。筛选条可清除。金额数据点进入账单或配送页，质量指标进入质量页；不声称目标列表能逐笔复算图表金额。图表空白、图例和纯说明不作为跳转入口。
- 生产审核、派工及生产订单/看板附件列表补齐非 STL 文件预览入口。格式白名单、私有文件鉴权及签名机制不变。

## 验证

- 前端 `pnpm --dir frontend build`、`vue-tsc -b`、`git diff --check` 通过。
- 最终 Chrome 真实浏览器回归 9 项全部通过（52 秒），包含实际上传和混合产品切换；不是仅检查截图或接口可达。
- 来源快照相关的两项旧检查改为验证当前摘要状态，不再因纠正历史中保留旧状态文字而误判。
- `node scripts/check-acceptance-refinements.mjs`：实际执行派工状态、409/403 文案、产品资料适用性、月份/客户/状态筛选、部门归类函数；附带模板门禁断言。
- 医生 final-confirmation、product-first、field-ownership、four-step-design-service 检查；客服导航、生产看板数据范围、管理工作台及验收账号 RBAC 检查。
- 后端独立测试库 `ai_order_refinements_20260903_test` / 独立文件桶 `ai-order-refinements-test-private`：`OrderRuleEngineTests` 17、`WorkflowRuntimeTests` 17、`CheckWorklogPerformanceTests` 21、`FileAccessTests` 20，共 75 项，0 失败、0 错误。日志 `/tmp/aiorder-refinements-backend-tests.log`。
- 首次后端运行被旧测试库 V98 checksum 差异拦截；未 repair、未删除或覆盖旧库，改用新独立测试库全新迁移重跑成功。
- Chrome 真实浏览器脚本：`DEMO_ISOLATED_ENV=true pnpm exec playwright test scripts/smoke-acceptance-refinements.spec.mjs --workers=1 --reporter=line`。检查派工页样式/已有执行人禁用安排、全测试账号检验入口、四端下方统计跳转、部门队列、客服已发货筛选、实际上传三份 STL / 手动分类 / 3D 预览、固定+弹性+隐形矫治混合产品资料入口。
- 浏览器初检发现筛选条落入侧栏列，已修正并复测；测试中的旧字段标签和看板选择器已按真实 DOM 修正，不跳过断言。
- 截图在 `test-results/refinements-*.png`，属于本地运行产物，不作为客户最终确认。

## 边界与交接

### 验收反馈修复：检验任务文字重叠

- 用户截图中的“待出检”长列表已在真实 Chrome 复现：受限高度网格把任务行压到 92px，换行内容超出行底部。原浏览器用例未检查多任务长列表的文字几何边界。
- 仅调整列表 `grid-auto-rows: max-content`、自动行高和长文本换行，不改字号、权限、状态筛选或检验逻辑。
- 新增布局回归修复前失败，修复后在 1728、1280、768px 三种视口检查所有任务文字不越界、不相互覆盖，末条任务可滚动选择。该项和既有派工/检验浏览器用例共 2 项通过；前端类型检查和构建通过。
- 截图：`test-results/inspection-layout-1728.png`、`inspection-layout-1280.png`、`inspection-layout-768.png`。测试未提交检验记录。

### 本轮边界

- 浏览器测试只在 `127.0.0.1:15173` / `18080` 的 demo 环境，创建测试草稿和 STL 附件；未提交这些草稿到生产流程，未执行浏览器派工写入，未触碰正式业务数据。测试产物保留供复查。
- 普通员工权限以角色权限检查和后端测试证明；本轮浏览器使用全功能验收角色，没有把普通员工浏览器测试伪称为已执行。
- 15 类文件所有变体、全部产品排列组合不属于本次浏览器穷举证据；恶意 Office/ZIP 处理及权限隔离由既有安全逻辑与文件回归覆盖。
- 没有提交、推送或部署。Task 8 保持 `NOT_READY`。本轮不启用实体模型、不补造 CBCT 子产品医学规则、不改派工范围。
