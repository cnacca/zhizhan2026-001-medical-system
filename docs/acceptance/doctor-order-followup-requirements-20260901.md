# 订单后续需求本地验收记录（2026-09-01）

## 验收口径

本记录只证明当前 `dev` 工作树的本地实现和自动化结果，不代表已提交、已发布、已写正式数据或已完成客户逐项签字。

| 需求 | 本地实现 | 自动化证据 | 真实四端浏览器 |
|---|---|---|---|
| 上传白名单文件全部预览 | 四端统一预览；图片/PDF、STL/SLA/PLY/OBJ、DICOM、文本/Office、ZIP 目录及安全成员分别处理 | `FileAccessTests`、前端生产构建 | 客服订单 STL 已在 Edge 中打开站内 3D 预览；15 类真实样本全组合仍需专项验收 |
| 四端工作台跳转 | 业务卡片和业务列表按角色带筛选跳转；订单行进详情；趋势图仅数据点下钻 | 前端生产构建、四端路由代码检查 | Edge 已验证医生“制作中”、客服订单数、生产“生产异常”和管理“生产异常”均跳到对应页面／筛选 |
| 医生新建订单资料上传 | 单一批量投放、自动分类、待分类补录；弹性义齿无侧面照；口扫两种完整性；选传/专属区折叠；隐形矫治无比色 | `check-task-034-order-rules`、前端生产构建 | Edge 已验证仅显示系统预计到货日期和口扫订单类型；完整上传组合仍以专项检查为主 |
| 医生补充资料 | 阶段化补充；说明选填；审核前生效，审核后客服审批，投产后审批并通知生产，终态禁止 | `OrderStatusProjectionTests` | 各阶段状态推进仍以自动化证据为主，未在浏览器写入验收数据 |
| 订单标识 | 四端完整订单号一致；内部工作人员保留派生系统尾号；生产单号永久唯一；盒号仅发货前唯一且撤销发货重检 | `OrderRuleEngineTests`、数据库迁移测试 | Edge 已在客服详情、生产订单和管理工序页看到完整订单号；生产页同时保留 6 位系统尾号 |
| 医生数据隔离和页面层级 | 只见公开节点；资料区以下默认折叠；设计确认独立于沟通 | 投影与权限回归、前端生产构建 | 医生端页面层级已抽验；敏感字段隔离以投影／权限测试为决定性证据 |
| 唯一预计到货日期 | 只保留系统预计日期；按受理时间、最长制作周期、运输天数计算；客服/管理员说明原因后覆盖 | `OrderRuleEngineTests`、OpenAPI 校验 | Edge 已验证医生新建页无可编辑要求到货日，客服详情仅维护系统预计日期且原因必填 |
| 生产组长受控派工 | 独立 `PROD_TEAM_LEAD` 生产账号；仅本部门有效生产人员/自己；无候选不扩权；支持改派 | `WorkflowRuntimeTests` | Edge 已验证权限入口、2 单可派工列表、逐工序选择和目标员工选择器；未实际提交派工 |
| 发货七天自动确认 | 保留医生手工确认；提前 24 小时提醒；7×24 小时幂等确认；非发货状态暂停 | `MessageDesignBillNotificationTests` | 时间推进需受控时钟专项验收 |

## 已执行并通过

- `MYSQL_TEST_DATABASE=ai_order_followup_fullb_test MINIO_TEST_BUCKET=ai-order-followup-fullb-test ./scripts/with-jdk21.sh mvn -f backend/pom.xml -pl platform-server test`：368 项，0 失败，0 错误。
- `pnpm --dir frontend build`：通过。
- `node scripts/check-task-034-order-rules.mjs`：64 项断言通过。
- `npm run check:openapi`：自定义检查、Swagger 校验和 Redocly 校验通过；保留 3 个既有非阻断警告。
- 全新 MySQL 8.4 数据库从基线迁移至 V98：通过。
- `git diff --check`：通过。

## 边界

- 实体模型／实体印模能力和生产端后续反馈不在本轮范围。
- 未提交、推送或部署，未修改正式订单与正式环境数据。
- Task 8 保持 `NOT_READY`。
