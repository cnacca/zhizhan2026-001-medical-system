# 一期客户 / PM 确认与外部输入分类清单

状态：ACTIVE / 2026-08-25 校正版。

本清单以 PRD V2.0（2026-07-04）和 2026-07-06 已确认范围基准为准，覆盖 2026-07-05 旧 DRAFT 中“CP-001 到 CP-009 全部逐项书面确认”的口径。历史编号继续保留，便于追溯；它们不再都代表产品决策或签字阻塞。

严格按原 PRD 与 CP-001～CP-009 计算：**待客户 / PM 确认 0 项，PRD 明确要求逐功能签字 0 项。**CP-002 动态表单和 CP-005 文件要求已经提供并形成基线，不再重复索取。AI-5 正式模板、标准工时，以及正式 AI Key／预算上限／告警接收人已由用户标记为 `USER_DEFERRED`，在用户主动恢复前不得继续询问或催办。项目交付时应在原验收表38项、2026-07-06 A/B/C 表外范围、跨项门禁、上线 readiness 与真实交付证据全部关闭后形成一份总体验收记录，不把38项测试误算为38次签字。

Task 8 仍保持 NOT_READY。原因应按真实的本地实现缺口、外部输入、真实环境执行和最终验收证据分别记录，不能继续统一写成“等待客户 / PM 签字”。

尚未发生的客户培训、真实环境执行和总体验收，其负责人、日期、记录编号与结果仍为待填写；这里的“待填写”是后置证据状态，不是开发前置签字。

## 分类规则

- `PENDING_CONFIRMATION`：PRD 明确要求 PM / 客户确认的产品参数。
- `CONFIRMED_BASELINE`：PRD 或 2026-07-06 基准已确定，直接按基准开发；如要改变才走范围变更。
- `CUSTOMER_INPUT_REQUIRED`：客户提供模板或资料，不是需求签字。
- `BUSINESS_DATA_REQUIRED`：需要真实业务数据；PRD 未指定提供方时，由项目方指定负责人协调，不是需求签字。
- `USER_DEFERRED`：输入仍未提供，但用户已明确暂缓；只保留影响说明，在用户主动恢复前不询问、不催办、不作为下一步推荐。
- `OUT_OF_PHASE_ONE`：不属于一期 P0；只有新增范围时才评估。
- `DELIVERY_EVIDENCE_PENDING`：开发完成后的培训或最终验收证据，不是开发前置决策。
- `EXTERNAL_ENV_EVIDENCE_PENDING`：真实环境部署、联调和运行证据，不是产品需求确认。

## 当前数量

| 类别 | 数量 | 当前事项 |
| --- | ---: | --- |
| 待客户 / PM 产品确认 | 0 | 无 |
| 用户暂缓资料 | 1 | CP-003 AI-5 正式生产备注模板；`USER_DEFERRED` |
| 用户暂缓业务数据 | 1 | CP-004 各工序标准工时；`USER_DEFERRED` |
| 用户暂缓环境输入 | 1 | 正式 AI Key、预算上限和告警接收人；`USER_DEFERRED`，不计入 CP-001～CP-009 数量 |
| 已由基准确认 | 4 | CP-001 一期人工付款；CP-002 动态表单；CP-005 文件要求；CP-007 一期人工物流 |
| 一期范围外 | 1 | CP-006 真实电子签章 / 复杂终检报告模板 |
| 最终交付证据 | 1 | CP-008 正式培训与签收记录 |
| 真实环境执行证据 | 1 | CP-009 真实部署、HTTPS、备份、监控和回滚记录 |
| PRD 明确要求逐功能签字 | 0 | 无 |

补充：PRD Q3 已给出签名 URL 默认值——预览 15 分钟、下载 2 小时，仅在实际使用反馈后调整。当前按已确认默认参数执行，不新增为待确认项；38 项中的 11.3-04 按预览 15 分钟验收。

## 历史 CP-001 至 CP-009 校正表

| 确认项编号 | 主题 | 当前分类 | 当前口径 | 对一期开发的影响 | 关闭方式 |
| --- | --- | --- | --- | --- | --- |
| CP-001 | 付款状态口径 | `CONFIRMED_BASELINE` | 已确认一期由 CS / ADMIN 人工维护、不接真实支付系统；V2 对外状态为未付、部分付、已付。当前代码的 `NOT_REQUIRED` 是内部实现扩展，不视为客户已确认的第四个对外状态。 | 不再阻塞人工付款闭环；仍需在后续代码验收中对齐三个 V2 对外状态。 | 按人工维护方式和 V2 三状态验收；若保留 `NOT_REQUIRED`，仅作为有明确业务依据的内部/例外状态并避免扩大对外口径。 |
| CP-002 | 动态表单最终字段 | `CONFIRMED_BASELINE` | 客户已提供《动态下单表最终版.docx》，SHA-256 为 `147810af568da193d7acd10f7963283ff8e0e74f5f272415aefda7bb6b1daef5`；字段、牙位、文件、过程确认和医生向导已整理到 `docs/requirements/dynamic-order-form-field-matrix-20260731.md` 并用于动态表单 V2 实现。 | 不再作为“资料未提供”阻塞。正式价格、交期等独立业务值另行跟踪，不能反向把 CP-002 重新打开。 | 以源文件指纹、字段矩阵和现有实现为关闭证据；后续变更另走确认。 |
| CP-003 | AI-5 生产备注模板 | `USER_DEFERRED` | AI-5 只生成草稿，人工确认后写入；用户已暂缓提供正式生产备注格式。 | 当前可用通用模板开发和测试，但不能宣称为客户正式模板。 | 用户主动恢复后再接收模板并复测 11.4-05；恢复前不询问或催办。 |
| CP-004 | 标准工时与绩效 | `USER_DEFERRED` | PRD 已定义完成数量、有效工时、返工次数、准时率、通过率、工时效率六项指标，并依赖各节点标准工时；用户已暂缓提供正式数据。现有加权 `performance_score` 不是 PRD 一期硬验收要求，不用于工资奖惩。 | 标准工时缺失时，11.1-11 不能做真实效率和准时率验收；其他工时链路继续开发，正式开关保持关闭。 | 用户主动恢复后再指定数据负责人、接收数据并按 PRD 六项公式核算；恢复前不询问或催办。 |
| CP-005 | 文件上传限制 | `CONFIRMED_BASELINE` | 客户确认文件要求均已在动态表单资料中提供；按现有表单／PRD 基线执行单文件 500MB、单订单最多 50 个、类型白名单和各产品资料规则。 | 不再作为客户输入缺口。2026-08-25 已将后端／测试默认、前端、开发／生产 env 模板和 Compose 统一为 50；完整发布脚本强制注入并复核容器值。 | 本地配置和边界回归通过后关闭实现缺口；正式发布后补容器值为 50 的证据，后续规则变更另走确认。 |
| CP-006 | 真实电子签章 / 复杂终检报告模板 | `OUT_OF_PHASE_ONE` | V2 一期没有真实电子签章平台硬要求；现有签名占位不能作为一期阻塞。 | 从一期 P0 readiness 中移除。 | 客户新增要求时建立变更单、PRD、工程量和独立验收。 |
| CP-007 | 真实物流平台 / 运单同步 | `CONFIRMED_BASELINE` | 一期人工录入承运商、运单号和状态；真实物流 API 属于二期或范围变更。 | 不再阻塞一期人工物流和医生确认收货闭环。 | 按人工物流验收；只有新增 API 接入时再确认平台。 |
| CP-008 | 客户培训与签收 | `DELIVERY_EVIDENCE_PENDING` | 操作手册和培训材料先准备，正式培训在功能验收后执行。 | 不阻塞本地功能开发；影响最终交付证据。 | 培训完成后记录参会人、日期、问题和签收结论。 |
| CP-009 | 真实环境上线验收 | `EXTERNAL_ENV_EVIDENCE_PENDING` | 真实服务器、域名、HTTPS、对象存储、备份、日志、监控和回滚需在目标环境执行。 | 不阻塞需求实现，但 Task 8 在真实环境验收前仍为 NOT_READY。 | 部署方提供环境资料并完成真实运行记录；敏感值不得写入仓库。 |

## 2026-08-02 客户验收反馈新增待确认项（GOAL-034）

来源：客户 2026-08-02 验收反馈核查（`goals/GOAL-034-customer-acceptance-feedback-closure-20260802.md`）。以下四项均为我方先拟默认口径、客户可调，不阻塞开发。

| 编号 | 主题 | 当前分类 | 我方拟定口径 | 关闭方式 |
| --- | --- | --- | --- | --- |
| CP-010 | 客诉率统计口径 | `PENDING_CONFIRMATION` | 客诉数 = 客服登记的外返（`quality_record.record_type = 'EXTERNAL_RETURN'`）；客诉率 = 客诉数 ÷ 出检订单数。与按责任方分类的外返率是不同事实，不重复计数。 | 客户确认或给出替换口径。 |
| CP-011 | 退货率 | `PENDING_CONFIRMATION` | 一期不启用，接口固定返回 `null`，界面显示「口径未启用」，不用 0% 冒充。启用前提是先引入「退货订单」产品类型（TASK-034 F 批次）。 | 客户确认一期是否必须提供退货率数值。 |
| CP-012 | 部门级外返 / 客诉拆分 | `PENDING_CONFIRMATION` | 当前不做。外返登记创建的返工记录不绑定工序节点，数据模型无法把外返归属到部门；部门表只展示可归属的任务量、内返率、出货率、完成达成率。 | 客户如确需部门拆分，需在外返登记表单增加「责任部门 / 工序」字段，属业务语义变更，另立需求。 |
| CP-013 | 牙科 FAQ 正式语料 | `CUSTOMER_INPUT_REQUIRED` | AI-6 的知识表结构由我方建好，先填入我方拟定的牙科通用条目，界面标注「示例内容，待甲方确认」。 | 客户提供正式 FAQ 条目后替换。 |

## 与38项验收的关系

- 38项全部是一阶段 P0 验收测试，不是38次签字。
- 6个验收项会使用上述外部输入：11.1-01、11.1-02、11.1-11、11.4-02、11.4-05、14.4-01。
- 其中 CP-005 已有 PRD 默认参数，因此上传能力应继续实现和测试；参数调整不应成为停工理由。
- 其余32项不依赖已暂缓的模板或标准工时数据，必须按本地代码和测试完成度直接核算。
- 逐项状态见 `docs/acceptance/prd-v2-38-item-acceptance-audit-20260715.md`。

## 当前待办

1. CP-002、CP-005 已关闭：不再要求客户重复提供动态表单或文件要求。
2. 完整发布后记录后端容器 `FILE_MAX_FILES_PER_ORDER=50`，不再要求客户确认参数。
3. CP-003、CP-004 及正式 AI Key／预算／告警接收人保持 `USER_DEFERRED`；用户主动恢复前不询问或催办。
4. 开发团队继续关闭不依赖上述输入的本地和真实环境缺口。
5. 原验收表38项、2026-07-06 A/B/C 表外范围、跨项门禁、上线 readiness 与真实交付证据全部关闭后，形成一份总体验收记录。
