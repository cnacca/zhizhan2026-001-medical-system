# 生产开工入口与反馈修复验收（2026-09-09）

状态：本地修复与点击验收通过，正式发布待执行。

## 范围与正式站只读证据

用户在回退上一轮改动后要求针对正式站“开始工作点击无反应”核实、修复并模拟真实用户点击验收。本次不恢复已撤回的追溯、设备材料、产品车金路线功能，不重写既有订单。

正式站原工单 ORD20260821-D378BE07E0：设计任务 #5 为已领取，尚未上传版本；生产详情把设计确认节点显示为待开工并给出开始工作按钮。已生成的后续 23 步可读取，状态为待处理。未对正式工单提交开工、完工或修改设计状态。没有捕获原工单 start 请求，不能将推断描述为该请求实测报错。

## 修改

- 设计确认节点显示设计流程处理中，入口改为处理设计任务、查看待领取设计；跳转过滤对应订单，并提供查看全部任务。无页面权限时在原抽屉内说明。
- 服务端 start/complete 拒绝把 DESIGN_GATE 当普通生产节点操作，包括管理员代操作；既有医生确认流程继续自动完成节点并激活后续工序。
- 工序详情 can_start 与现有执行权限、设计确认、入检条件一致，返回明确阻塞原因；不扩大派工权限。
- 两处生产抽屉原位显示失败、成功和开工限制，提交中锁定按钮；无节点不再静默返回。完整流程与原有布局保留。
- 无数据库迁移，不修改历史数据和医生端字段。

## 验证

- 全新隔离库 ai_order_startfix_final_20260909_test：370 项后端测试，0 失败、0 错误、0 跳过。新增测试验证设计节点不能经普通开始/完成接口推进，以及派工和设计确认原因。
- 前端类型检查与构建通过；OpenAPI 通过（3 条原有警告）；生产看板专项通过。发布门禁结果见后续发布记录。
- 第一次全量运行新增测试的管理员 fixture 缺少读取权限产生 403；修正测试身份权限后，在全新库重跑通过，未放宽业务权限。

## 浏览器实际点击（仅本地 15174 / 18081）

Chrome：看板打开 DESIGNFIX20260909-UI，设计节点显示处理中、没有开始工作；点击处理设计任务，进入我的设计任务并只显示本单 #595，已领取、尚未上传。

Edge：打开 STARTFIX20260909-UI。用本次专用测试数据模拟页面打开后工序被改派，再实际点击开始工作，详情内显示“当前账号无权操作这道工序，请由负责人处理或联系生产组长。”，未生成开始时间。将测试 fixture 恢复负责人后再次点击开始工作，界面变为进行中且有成功提示；点击标记完成成功，后续入检未通过节点显示具体原因和禁用的暂不可开工按钮。

数据库核实本次真实点击结果：开始 2026-09-09 10:59:52.172，完成 10:59:59.540；设计节点仍 READY、开始/完成均 NULL；后续节点 READY 且未开始。测试 fixture 通过本地 SQL 准备，不冒充从下单到设计审核的完整 UI 验收。

Edge：生产订单列表打开 DESIGNFIX20260909-UI，第二处详情同样显示设计流程处理中、处理设计任务及完整说明。

浏览器控制期间有窗口焦点和截图捕获异常，切换 Edge 后完成上述点击。未用接口调用冒充用户点击，未声称高并发或全部视口已人工验收。

## 部署与回退

按既有 fix 分支 → dev → main 的 PR 发布流程；不直接 push main，不手工改正式数据。无新增迁移，可回退到前版镜像；本地旧测试库 V92/V93/V99/V100 兼容文件仅在仓库外启动脚本加载，不随代码发布。Task 8 保持 NOT_READY。
