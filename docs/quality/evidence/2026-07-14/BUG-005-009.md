# BUG-005 至 BUG-009 修复与回归证据

## 基线

- 工作树：`/Users/yuri/Documents/AI智能下单平台`
- 分支：`feature/project-skeleton`
- 修复父提交：`b1b6885c`
- 运行日期：`2026-07-14`
- 启动方式：`npm run demo:serve`
- 服务地址：前端 `http://127.0.0.1:15173`，后端 `http://127.0.0.1:18080`
- 数据差异：当前隔离演示库保留 7 条脱敏场景订单；用户提供的原 1013 三个 STL 已通过分片上传绑定到脱敏完成订单 #7，并新增一条带 `DEMO_DATA_V1` 标记的脱敏留言。未修改或重置真实数据。

## 修复摘要

- BUG-005：只接受上传完成且扩展名精确为 `.stl` 的文件；优先医生原始 `ORDER_ATTACHMENT`；增加显式 STL 选择器、无文件禁用态和基于 Three.js/STLLoader 的交互式 3D 查看器。真实浏览器回归又发现高分屏像素比使 WebGL 画布按内部缓冲区尺寸参与 CSS 布局、页面只显示左上四分之一，现已把缓冲区尺寸和 CSS 显示尺寸分离，三个模型均完整居中显示。
- BUG-006：医生订单投影新增安全布尔字段 `editable`，仅草稿、客服驳回、生产驳回可编辑；完成订单不再显示编辑入口，编辑动作增加前端守卫并正确切换到新建/补资料页面。
- BUG-007：医生订单工作区和客服订单详情加载 `/orders/{id}/files`，只展示对当前角色可见且来源为 `ORDER_ATTACHMENT` 的原始附件。
- BUG-008：医生 `/collaboration` 路由改走医生专用加载；消息主请求与可选的 @ 人员请求解耦；服务端补充“医生可读取本人已发送的 `CS_ONLY / DIRECT` 留言”规则，同时保持生产端不可见。
- BUG-009：生产端牙位映射优先读取 `tooth_position`，并保留旧字段兼容。
- 文件打开行为：预览/下载先在点击手势内同步创建等待窗口，再异步获取短时效签名地址，避免签名请求完成后才 `window.open` 被浏览器当作非用户操作拦截。

## 定向验证

- `npm run check:feedback-20260713`：通过，覆盖 5 项缺陷的静态门禁。
- `OrderStatusProjectionTests`：14/14 通过，覆盖 `editable` 的允许/禁止状态和医生投影脱敏。
- `MessageDesignBillNotificationTests`：15/15 通过，覆盖医生本人留言历史、客服可见性和生产端隔离。
- 原 1013 STL 数据回证：三个文件均为 `ORDER_ATTACHMENT / DOCTOR / COMPLETED`；医生、客服、生产三角色各读取到 3 个文件；预览/下载签名 URL 均可用，Range 请求均返回 `206`。
- 文件完整性：从签名 URL 取回的三个完整文件与用户原文件大小及 SHA-256 全部一致；前端同版本 `STLLoader` 分别解析出 528785、321580、38009 个三角面。
- 留言数据回证：修复前同一留言表现为“客服可见、医生不可见”；服务端修复并重启后为“医生可见、客服可见、生产不可见”。
- 真实浏览器：
  - 医生端完成订单显示锁定提示，行内和抽屉均无编辑按钮。
  - 医生端完成订单详情显示原 1013 的 3 个 STL 文件名、大小和预览/下载按钮；实际点击预览后未出现弹窗拦截反馈。
  - 客服端完成订单详情显示同一组 3 个“医生原始附件”；按订单号查询沟通中心可看到脱敏回归留言正文。
  - 医生沟通中心实际显示 `DEMO_DATA_V1：BUG-008 医生留言历史回归（2026-07-14）`，发送人、直接发送方式均正确。
  - 生产端列表与详情牙位均显示 `16`；STL 选择器严格只列出 Mandibular、Maxillary、Normal Bite 三项，不混入账单或设计文本。
  - 依次打开三个真实 STL 后均完整生成不同模型；Normal Bite 实际执行拖动旋转和滚轮缩放，画面随操作变化；生产端实际点击选中 STL 下载且无弹窗拦截反馈。
  - 医生、客服、生产三端控制台错误日志均为空。

## 关联回归

- `npm run build:frontend`：通过。
- `npm run check:openapi`：通过。
- `npm run check:production-board-reference-clone`：通过。
- `npm run acceptance`：通过。

## 回归结论

- BUG-005 至 BUG-009 已全部完成代码、接口和真实浏览器交互回归并关闭。
- 本结论只覆盖本次 1013 反馈的五项缺陷和隔离演示数据，不改变正式上线条件；Task 8 继续保持 `NOT_READY`。

## 已知历史检查漂移

- `npm run check:task9d58` 和 `npm run check:task9d88` 仍失败：两个历史脚本分别要求 `acceptance.json` 包含 `task-9d58-customer-collaboration-required-text`、`task-9d88-customer-collaboration-visibility-required-text` 占位字符串。
- 两个字符串在修复父提交 `b1b6885c` 的 `acceptance.json` 中已不存在，本轮也未修改该文件；不通过补无业务含义的占位文本伪造通过。
