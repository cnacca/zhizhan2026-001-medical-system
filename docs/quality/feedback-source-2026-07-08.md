# 2026-07-08 测试反馈来源记录

## 来源与使用限制

- 原始包名：`2026-07-08_给开发_正式流程测试反馈_v5(1).zip`。
- 原始测试基线：旧版 `feature/project-skeleton`，反馈记录的提交为 `a744aeaacb0b96f1ae7bd1075a76ec14f614b3f1`。
- 当前工作树不是该旧基线；任何当前结论必须重新运行后写入 `defect-tracker.md`。
- 原始 zip 位于微信临时目录，2026-07-10 建立本记录时该临时路径已不可访问。本文件依据此前已读取的包内 Markdown、日志、JSON 和截图摘要重建，不宣称逐字保存原文或原截图。
- 原包内可定位的材料包括：`给开发的问题反馈_2026-07-08.md`、`缺陷记录表.md`、`阶段二_正式流程测试结果.md`、`menu_smoke_results.json`、`smoke_task9d24.log`、`mismatched_portal_api_check.log`、`static_check-scope-baseline-20260706.log` 和页面截图。

## 旧版测试总体结论

- 环境启动、四端正常账号登录和 12 步主链路 smoke 在旧基线上通过。
- 旧版并未达到最终验收；Task 8 仍应保持 `NOT_READY`。
- 反馈归纳了 4 项开发问题：BUG-002（P1）、BUG-001（P2）、BUG-003（P2）、BUG-004（P2）。

## BUG-002：医生账号与管理端入口隔离 [P1]

- 场景：从管理端入口使用医生账号登录。
- 预期：拒绝登录，显示账号角色与所选入口不匹配，不进入管理端或其他工作台。
- 旧版观察：四端正常登录通过；错误入口的提示断言未找到，失败上下文落在医生工作台。
- 旧版补充：接口层记录过 `/api/auth/login` 的 `403 Forbidden`。因此当前核验必须同时检查接口响应、前端失败处理、旧登录态、成功跳转和路由守卫。

## BUG-001：范围基线检查缺少 C 类基础能力 [P2]

- 旧版命令：`npm run check:scope-baseline-20260706`。
- 旧版错误：`acceptance.json -> missing C 类基础能力`。
- 当前核验重点：区分真实能力缺失、文档漏写和历史检查断言漂移；不得通过降低一期 C 类要求或 Task 8 边界关闭问题。

## BUG-003：菜单入口为演示、待接入或接口错误 [P2]

旧版菜单 smoke 共记录 20 项异常。以下清单只描述旧版观察，当前版本必须逐项重新判定。

| 端 | 旧版观察 |
| --- | --- |
| 客服端 | 外协管理、生产备注助手：占位、演示或待接入。 |
| 生产端 | 扫码登记、终检报告、奖惩管理、设备管理、物料管理、成本管理、外协成本、安环管理、消息中心、云端数据中心：占位、演示或待接入。质量总览：接口/权限/服务错误，截图记录为 `403`。 |
| 管理端 | 用户管理、角色权限、设备管理、物料管理、外协管理、账单配送：占位、演示或待接入。绩效统计：接口/权限/服务错误，截图记录为 `400`。 |

- 范围提醒：旧反馈不自动授权把全部菜单扩展为完整一期闭环。每项只能判定为已实现、范围内未完成、经确认不属于一期范围，或独立权限/接口缺陷。

## BUG-004：管理端绩效统计请求返回 400 [P2]

- 旧版观察：页面可进入，但显示请求失败 `400` 和“暂无绩效统计”。
- 当前核验重点：前端默认查询参数、日期/员工筛选、后端参数校验和角色/数据范围；有数据与无数据的页面状态应分别验证。

## 与当前台账的对应关系

| 旧反馈 | 当前处理入口 |
| --- | --- |
| BUG-002 | `defect-tracker.md` 的 BUG-002；优先级 P1。 |
| BUG-004 | `defect-tracker.md` 的 BUG-004。 |
| BUG-001 | `defect-tracker.md` 的 BUG-001。 |
| BUG-003 | `defect-tracker.md` 的 BUG-003；菜单清单以本文件为输入。 |
