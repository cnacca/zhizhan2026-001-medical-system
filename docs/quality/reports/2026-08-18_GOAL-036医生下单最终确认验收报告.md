# GOAL-036 医生下单最终确认验收报告

## 结论

- 代码实现与自动化验收：`PASS`。
- 真实 Chrome 文件上传与资料回显路径：`PASS`；最终提交未在浏览器点击，提交门禁由后端集成测试覆盖。
- 正式价格、真实环境文件验收、生产部署：未执行。
- Task 8：`NOT_READY`。

## 需求验收

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 上颌、下颌、咬合三必传 | PASS | 前端固定槽位和后端 `FIXED_SHARED_V1` 提交校验 |
| 其他资料选传 | PASS | 比色、口内咬合照、旧义齿参考空值可通过后端集成测试 |
| 拖拽与点选 | PASS | 前端实现 drag/drop；2026-08-21 已在隔离环境完成真实 Multipart 上传与回显 |
| 多产品共享文件 | PASS | 病例组共享 file IDs 和每个 item 的槽位映射 |
| 最终页八类信息 | PASS | 产品级摘要结构与前端构建 |
| 价格占位 | PASS | 未计价时显示“价格待配置”，不显示 0 或提交后报价文案 |
| 询问客服不阻止提交 | PASS | 复用现有 `sendMessage` 链路，询问状态不在提交 disabled 条件中 |

## 自动化证据

- `npm run check:doctor-order-final-confirmation`：PASS。
- `npm run check:doctor-order-product-first`：PASS。
- `npm run check:doctor-portal-i18n`：PASS。
- `npm run build:frontend`：PASS。
- `scripts/with-jdk21.sh mvn -f backend/platform-server/pom.xml -DskipTests compile`：PASS。
- 2026-08-21 新建隔离数据库和测试 bucket 上运行 `OrderCaseGroupTests`：9 tests，0 failures，0 errors。
- `npm run acceptance`：PASS。
- `git diff --check`：PASS。

默认测试库存在既有 V73 Flyway checksum 不一致，在应用启动前阻断；本轮未执行 Flyway repair，而是使用全新隔离库完成目标测试。

## 浏览器证据与剩余边界

2026-08-21 使用全新隔离数据库和测试 bucket 重跑后，Chrome 已完成 3 个病例共享 STL 上传；两个产品均正确继承三项共享扫描。随后为其中一个产品上传 1 个同类专属 STL，仅该产品切换为专属资料，另一产品继续继承共享资料；最终确认页分别显示“共享 3 个，专属 0 个”和“共享 3 个，专属 1 个”，控制台无错误。

浏览器未点击最终提交，避免制造非必要订单；共享／专属组合、必传资料、色号和材料等提交门禁由 `OrderCaseGroupTests` 覆盖。正式价格和真实生产环境文件验收仍未执行，Task 8 保持 `NOT_READY`。
