# 一期交付缺口清单（当前权威）

状态：ACTIVE / 2026-08-25 证据校准

## 一、校准结论

正式站已经部署并持续通过自动发布，不能再沿用“服务器未就绪、HTTPS 完全没有、医生端英文覆盖约 1%、
备份完全没有、8088 尚未重新部署”等 2026-08-11 旧结论。

当前最强正式证据：

- 正式版本：`7013b1434759df6ca77a65893dbdcf0129e9af7b`。
- 自动发布：#32742027332，结果 `success`，耗时 10m21s。
- 发布模式：D-203 首次启用因包含工作流／脚本，按预期走 `full`。
- 验证：后端 358 项测试、release gates、前后端不可变镜像校验、MySQL 部署前备份、双容器替换、
  后端／首页健康检查和公网 CORS／规范重定向探针全部通过。
- 公网：`https://chinesedigitaldental.com/` 与 `/api/bootstrap/health` 返回 200。
- 医生端英文：D-197 已发布并做正式站只读复验。
- 文件查看：D-202 已发布；客服、管理端和管理端生产复核页有 STL 站内 3D 点击证据，
  图片／PDF 站内预览，其他格式仅显式下载。

Task 8 仍为 `NOT_READY`。已经上线只关闭“部署存在、HTTPS 可达、发布前 MySQL 备份可生成”等事实，
不自动关闭恢复演练、MinIO 对象备份、监控告警、真实 AI、真实弱网／跨设备、客户培训和总体验收。

## 二、验收账

### 原 PRD V2 38 项

| 状态 | 数量 | 当前含义 |
| --- | ---: | --- |
| `PASS` | 29 | 本地代码、自动化、smoke 或后续正式发布证据已覆盖 |
| `PARTIAL` | 1 | 绩效统计缺正式标准工时业务数据 |
| `MISSING` | 0 | 原 38 项当前无本地代码缺口 |
| `EXTERNAL_ACCEPTANCE` | 8 | 真实模型、对象存储、网络、时间或正式环境条件下待验 |

逐项依据：`docs/acceptance/prd-v2-38-item-acceptance-audit-20260715.md`。
2026-08-25 按 38 行实际状态复核：29 PASS、1 PARTIAL、0 MISSING、8 EXTERNAL_ACCEPTANCE。

原 38 项、2026-07-06 A/B/C 表外范围、跨项流程门禁、上线 readiness 和最终交付证据必须分别跟踪。
不能只凭其中一张表宣称整个一期完成，也不能把客户资料输入统一写成逐功能签字。

### 2026-08-25 测试报告后续范围

该清单是原 PRD 38 项之外的后续反馈，单独分账，不改写上表 29／1／0／8：

| 项目 | 当前处理 |
| --- | --- |
| 1. 医生下单仍为 6 步 | 当前一期，GOAL-037／TASK-039 先实施 6 → 4 步；只合并页面分组，字段和校验不删 |
| 2. 完整 CRM／客户关怀提醒 | 用户确认转入二期，不作为当前一期阻塞 |
| 3. STL 直接打开 CAD／设计软件 | 用户确认转入二期，不作为当前一期阻塞 |
| 4. “推簧”目录映射 | 当前一期，排在第 1 项之后；先核对是否为现有产品／配件别名，禁止猜测建重复 SKU |

### 已校准掉的旧冲突

| 旧结论 | 当前判定 |
| --- | --- |
| HTTPS 完全没有 | 已关闭：正式域名 HTTPS 可访问，公网探针通过 |
| 客户仍在采购服务器／真实服务器未部署 | 已关闭：正式站已有连续发布运行证据 |
| 医生端国际化覆盖约 1% | 已关闭：D-197 已发布并完成正式站关键路径只读复验 |
| 数据库备份完全没有 | 部分关闭：完整发布会生成并校验 MySQL 备份；恢复演练、保留策略仍未完成 |
| 备份全部完成 | 不成立：MinIO 对象备份／恢复和异地保留没有当前完成证据 |
| 原 38 项仍有 4 MISSING / 8 PARTIAL | 已过时：逐行复核后的当前基线为 29 PASS / 1 PARTIAL / 0 MISSING / 8 EXTERNAL_ACCEPTANCE |
| 生产端 STL 完全未验证 | 不成立：管理端生产复核已有点击证据；普通生产账号因无分配工单仍为 `PARTIAL` |

## 三、当前九类 readiness 缺口

以下 ID 与 `acceptance.json.task8_readiness_gaps` 一致，全部保持 `PARTIAL`。

### auth-datascope-prod

已有 Bearer、refresh/logout、权限码、DataScope、严格 prod 门禁和角色越权回归；正式发布全量测试通过。
仍缺完整 Spring Security/JWT 或等价生产化方案、通用 SQL DataScope、access token 撤销／黑名单和多设备会话策略。

### frontend-business-pages

原 38 项没有本地 `MISSING`；四端主体、医生端英文、产品目录、文件预览和 STL 查看器均已上线。
测试报告后续新增的医生下单 6 → 4 步和“推簧”映射按 GOAL-037／TASK-039 单独推进；另需普通生产账号在有分配工单时补 3D 点击证据，并把真实环境／客户参数相关页面路径纳入最终总体验收。

### websocket-notification-prod

单实例 WebSocket、通知 REST、Redis 广播代码和 Nginx HTTPS 公网入口已有实现／探针。
仍缺真实双后端实例 Redis 联调、心跳／重连压测、生产 webhook 和监控告警证据。

### file-upload-prod

本地 105MB Multipart／中断恢复 smoke、正式 MinIO 签名预览和预览／下载分流已有证据。
客户已确认文件要求均包含在动态表单资料中：单文件 500MB、单订单最多 50 个、类型白名单和
各产品资料要求已有基线。2026-08-25 已把后端／测试默认、前端限制、开发／生产 env 模板、
Compose 默认和 README 全部校准为 50；完整生产发布还会强制注入 50，并在后端容器启动后复核。
本地配置／代码缺口已关闭，正式站需在本批完整发布后取得容器值为 50 的运行证据。
仍缺真实物理弱网、真实跨设备、恢复后对象大小／哈希核对、15／16 分钟签名过期实测、
测试／正式账号隔离证据和 MinIO 对象备份／恢复。

### ai-production-governance

DeepSeek 适配、LangChain4j、限流、预算、审计、熔断、输出保护和安全回归已实现。
当前代码仍只从环境变量读取 `DEEPSEEK_API_KEY`，没有客户追加要求的管理端安全配置入口；
真实 Key、真实 AI-1～AI-5 质量／安全／降级验收及生产 webhook 仍未完成。正式 AI Key、预算上限和
告警接收人已标记为 `USER_DEFERRED`；本地安全配置底座可继续，真实联调仅在用户主动恢复输入后执行。

### deployment-infrastructure

正式服务器、HTTPS、自动发布、部署锁、不可变镜像、SHA-256、部署前 MySQL 备份、回滚镜像标签、
健康检查和公网探针已经存在。仍缺数据库真实恢复演练、MinIO 备份／恢复、备份保留／异地策略、
容器资源限制、JVM 参数、日志轮转、监控告警和真实发布回滚演练。

### operations-manuals

四端操作手册、故障处理、回滚和培训模板已存在。仍缺按当前正式版本执行的生产手册记录、
客户培训签到／签收、回滚与恢复演练、监控告警接收人及正式运维移交。

### prd-v2-local-feature-gaps

旧的“4 MISSING / 8 PARTIAL”已被 2026-07-15 收口覆盖；2026-08-25 逐行复核后，原 38 项当前是 29/1/0/8。
本项继续 `PARTIAL`，用于承接 2026-07-06 A/B/C 表外基础能力、客户正式业务值和最终总体验收，
不得再次把已关闭的内部字段、自动选链、账号创建、设计确认或 OUT/PASS 门禁列成缺失。

### customer-pm-confirmations

CP-002 动态表单资料已经收到：《动态下单表最终版.docx》以 SHA-256
`147810af568da193d7acd10f7963283ff8e0e74f5f272415aefda7bb6b1daef5` 留痕，已整理为
`docs/requirements/dynamic-order-form-field-matrix-20260731.md` 并作为动态表单 V2 实现基线，不再要求客户重复提供。
CP-005 文件要求也已经收到：单文件 500MB、单订单最多 50 个、类型白名单和各产品资料要求按
现有表单／PRD 基线执行，不再要求客户重复确认。原 CP-001～CP-009 当前没有待客户／PM 产品
参数确认项；AI-5 正式模板和标准工时业务数据已由用户标记为 `USER_DEFERRED`。正式价格、交期等继续按独立业务参数跟踪，
不倒退写成“动态表单／文件要求未提供”。
正式 AI Key、预算上限和告警接收人同样为 `USER_DEFERRED`；在用户主动恢复前不再询问或催办。
培训和总体验收属于环境／交付输入，不统一伪装成逐功能签字。

## 四、按责任方拆分

### 我方可直接推进

1. 按 TASK-039 先完成医生下单 6 → 4 步，再核对“推簧”目录映射。
2. 管理端 AI key 安全配置：加密存储、运行时热加载、权限审计、不可明文回读。
3. 容器资源限制、JVM 参数、日志轮转和监控告警。
4. MinIO 对象备份方案及非生产恢复演练。
5. 双实例 WebSocket／Redis 联调环境和自动化记录。
6. GitHub Actions 浅拉取与弃用 action 升级；该项改善发布效率，不作为 Task 8 功能完成证据。

### 需要真实环境／合适数据

1. 普通生产账号有分配工单时完成 STL 3D 点击回归；不得写正式数据强造。
2. 大文件真实弱网／跨设备恢复、哈希无损和签名过期验收。
3. 真实 DeepSeek key 下完成 AI-1～AI-5 验收。
4. 数据库恢复、MinIO 恢复、发布回滚和告警触发演练。
5. 客户培训和最终总体验收记录。

### 用户已暂缓的外部输入

以下三项均为 `USER_DEFERRED`；在用户主动恢复前不得继续询问、催办或作为下一步推荐：

1. AI-5 正式生产备注模板。
2. 各工序正式标准工时；暂缓期间不得启用正式标准工时开关。
3. 正式 AI Key、预算上限和告警接收人。

CP-002 动态表单和 CP-005 文件要求均已收到；不得再次把它们列入待客户提供清单。

## 五、明确不作为当前一期阻塞

- 完整 CRM／客户关怀提醒：用户确认转入二期。
- STL 直接打开 CAD／设计软件：用户确认转入二期；外部协议、客户端、授权和审计未确认。
- 真实支付网关、真实物流 API、电子签章、RAG／tool calling、复杂 HR／工资结算。
- 为取得线上证据而制造正式订单、工单、患者或文件。

## 六、推荐执行顺序

1. 先提交并完整发布本批文件限制修复，补正式容器值为 50 的证据。
2. 无外部依赖时完成管理端 AI Key 安全配置底座与运行资源／日志／监控补强，但不要求真实 Key。
3. 完成普通生产账号 3D、文件、WebSocket、备份恢复和回滚演练；真实 AI 验收等待用户主动恢复输入。
4. 汇总原 38 项、表外范围、readiness 与培训证据，形成一份最终总体验收记录。

任何一步未取得真实证据时保持 `PARTIAL`；Task 8 只有在上述交付证据关闭后才能改为 READY。
