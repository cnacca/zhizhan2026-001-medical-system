# 交付缺口清单（唯一权威）

状态：ACTIVE / 2026-08-04
入口：`docs/INDEX.md`

## 这份文件的定位

`readiness-checklist.md` 与 `task-8-final-readiness-report.md` 是**追加式历史日志**，
开头堆着 2026-07 的阶段锚点，读起来像是还有几十项没做。
**当前真实缺口以本文件为准**，机器可读版本是 `npm run check:task8-readiness-gaps`。

---

## 一、已经完成的（不用再操心）

| 范围 | 状态 |
| --- | --- |
| PRD V2 原 38 项验收 | **30 PASS / 1 PARTIAL / 7 EXTERNAL_ACCEPTANCE**，无 MISSING |
| 四端功能主体 | 医生端下单、客服审核、生产工序/质检/返工、管理端配置与看板，全部有实现与测试 |
| 权限体系 | 20 个细分角色 + 权限码 + 数据范围，全部可配置，新增角色不改代码 |
| 管理端 RBAC 控制台 | 可实操：角色/权限/组织/人员/权限矩阵/授权留痕 |
| 下单规则后端化 | 试戴、过程确认、订单周期、运输类型、交期引擎，全部有可验证后果 |
| 导出管控 | 敏感数据审批 + 全量留痕；医生端无任何导出入口 |
| 账号交接 | 只转当前负责关系，历史事实一律不动，有分类守卫 |
| 后端测试 | 333 项，干净测试库上全绿 |
| 接口契约 | OpenAPI 194 paths / 223 operations，校验通过 |

---

## 二、真正还差的（按性质分四类）

### A 类：卡在「没有真实环境」——部署完就能关掉

这一类**不是代码没写**，是缺真实环境下的验收证据。部署上线后逐条补齐即可。

| # | 缺口 | 关闭条件 |
| --- | --- | --- |
| A1 | 部署 smoke / HTTPS / 备份监控记录 | 真实环境部署后按 9D.81 模板填 |
| A2 | 文件上传人工验收 | 真实对象存储 + 真实网络后按 9D.79 模板填 |
| A3 | WebSocket 双实例联调、Nginx HTTPS smoke | 真实环境后补 |
| A4 | AI-1 ~ AI-5 真实模型验收 | 接真实 AI key 后按 9D.80 模板填 |
| A5 | 客户培训签收、发布回滚演练、备份恢复演练 | 真实环境 + 客户配合 |

### B 类：卡在「客户没给数据」——客户给了就配置，不改代码

| # | 缺口 | 影响 |
| --- | --- | --- |
| B1 | **各产品标准制作周期** | 交期引擎现在用占位值，界面标「待确认」 |
| B2 | 国际快递在途天数（按国家/地区） | 同上。当前占位值是国内快递的 2 天，对海外严重偏低 |
| B3 | 各工序标准工时 | 绩效统计的 1 项 PARTIAL 就是这个 |
| B4 | 真实部门/班组/岗位、人员名单 | 现在是演示数据 |
| B5 | 产品价格 | 现在全部「待报价」 |
| B6 | CP-002 动态表单最终字段、CP-005 文件规则、CP-003 AI-5 模板 | PRD 原本就登记的客户输入项 |

### C 类：真实的代码/工程缺口——**需要我们动手写**

| # | 缺口 | 严重度 | 说明 |
| --- | --- | --- | --- |
| **C1** | **HTTPS 完全没有** | 🔴 硬门槛 | `frontend/nginx.conf` 只有 `listen 80`。医生端传患者资料与病例照片，明文不能上线 |
| **C2** | **备份完全没有** | 🔴 硬门槛 | 数据库与对象存储都没有备份脚本，也没做过恢复演练 |
| **C3** | **国际化实际覆盖率约 1%** | 🔴 见下 | 医生端两个主文件共 **1121 个去重中文硬编码**，双语函数 `uiText()` 只调用 **12 次**。语言切换按钮存在但基本不起作用 |
| C4 | 登录接口无失败锁定 / 限流 | 🟠 | 系统即将暴露公网，登录口没有暴力破解防护（只有 AI 网关做了限流） |
| C5 | 无安全响应头 | 🟠 | nginx 未配 HSTS / X-Frame-Options / CSP |
| C6 | compose 无资源限制、后端镜像无 JVM 堆参数 | 🟠 | 五个容器互抢，JVM 默认取容器内存 1/4，行为不可预期 |
| C7 | 无监控告警、无日志轮转 | 🟠 | 容器挂了没人知道；日志会撑爆磁盘 |
| C8 | 认证是自研 HMAC token，非标准 JWT，无 Spring Security | 🟡 | 能工作且有测试；缺 token 撤销/黑名单。密码用 PBKDF2-SHA256 / 120k 次迭代 / 16 字节盐，算法本身合规 |
| C9 | 对象存储用 MinIO（AGPL 授权） | 🟡 | 若走腾讯云 COS 可一并解决授权顾虑；需实测 COS 桶名格式兼容性 |

#### 关于 C3（国际化）——这是目前最大的未登记缺口

**它不在任何现有清单里**，因为 PRD 只写了一句「中文优先，i18n 可切换」，
而 38 项验收表里没有对应条目。但客户已明确说**医生是外部客户、配送基本发海外**。

实测数据：

| | 数量 |
| --- | --- |
| 医生端主页面去重中文字符串 | 719 |
| 下单向导去重中文字符串 | 402 |
| 双语函数 `uiText()` 实际调用 | **12** |
| 前端 i18n 框架（vue-i18n 等） | **未引入** |
| 后端返回给医生的中文提示 | 存在（仅下单规则一个包就 20 处） |

**如果海外医生看不懂中文，这是硬阻塞，工作量以周计。**
需要引入 i18n 框架、抽取文案、翻译、并处理后端返回的提示信息。

### D 类：规则口径待客户确认——已做成可配置，不阻塞

RULE-1 ~ RULE-6，见 `deployment/customer-confirmation-checklist-20260804.md` 第四节。
全部是开关或配置，客户回复后改配置即可，不改代码不发版。

---

## 三、优先级建议

| 优先级 | 事项 | 依赖 |
| --- | --- | --- |
| **P0** | 本地全链路演练（阶段一） | 无，立即可做 |
| **P0** | C1 HTTPS + C2 备份 | 域名证书到位 |
| **P0** | 确认 C3 国际化是否必须（**待澄清**） | 客户回答医生语言 |
| P1 | C6 资源限制与 JVM 参数 | 随阶段一一起做 |
| P1 | C4 登录限流 + C5 安全响应头 | 公网暴露前必须 |
| P1 | B1/B2 交期数据 | 客户提供 |
| P2 | C7 监控告警与日志轮转 | 部署后 |
| P2 | A 类真实环境证据 | 部署后逐条补 |
| P3 | C8 认证体系升级、C9 对象存储切换 | 非阻塞，可迭代 |
